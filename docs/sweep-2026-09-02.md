# The 2026-09-02 sweeps — the canon file, the thirteenth beat, and (appended 2026-09-08) the DOM namespace

Append-only, dated, superseded in place. *(Line-cites in this file are as read on each record's date, or as stamped beside them — a dated record keeps the numbers it read; only the lines that landing itself added to this file cite the tree it produced. The 2026-09-10 landing of counsel's rulings moved lines in canon, `KILLS.md` and eight beats without re-pointing the records above; the old-to-new table is in that landing's commit body, and the drift is the ledger's entry 39.)* This document holds three records the
2026-09-01 name-collision ledger (`docs/name-collisions-audit-2026-09-01.md`)
does not: the sweep of `docs/mechanisms-recommitted.md` against the sources it
cites, the sweep of `docs/cfd-176-beat.md` against `/dawnspur-heat/`, which
that ledger's §6 recorded as its one acknowledged gap, and — appended 2026-09-08
at the end, so nothing above it moved — the sweep of the DOM namespace that the
ledger's §3.2 recorded as "only spot-grepped" *(superseded 2026-09-08; this read
"holds two records")*.

**Both were produced by adversarial pairs — one sweeper and one refuter per
section, read-only, driving each shipped sim from `createBoard({fresh:true})` —
and both were checked by the orchestrator before their edits landed.** What
landed is named under each record's *Dispositions*. What is David's is listed
and unbuilt; nothing here changes a RULED or SIGNED sentence's meaning.

**Verified by the orchestrator, not taken on the agents' word**, before the
canon edits landed at `b8d6da3`: the R8 World Bible paraphrase, against both
World Bibles in the PWA tree; the Economy quotation, whose replacement text was
re-checked word for word against `Skyrail-Reclamation-How-the-Economy-Works.md:47`
because a correction that introduces a second misquote is worse than the defect;
the `:336` first-cumulative-board claim; and the `:248` lever count, driven over
all nine boards passed at HEAD. Before the CFD-176 edits landed at `b232440`:
every cited sha, the three MANIFEST hashes, both ancestry claims, two canon line
ranges and two test line ranges, with the commit timestamps converted from the
local `-0400` git log to the Z the beat states.

**One routed finding in the canon record is wrong and is corrected here rather
than in place, because the record is evidence of what was found:** it routes the
Economy splice's propagation to `docs/cfd-203-beat.md:259`. That file carries no
occurrence of the phrase at any tree today. The propagation is at
`docs/cfd-183-beat.md:259` — right line, wrong file — and was corrected at
`5a5dc34`.

---

# The canon sweep of `docs/mechanisms-recommitted.md` — 2026-09-02

The 2026-09-01 audit swept the twelve beat files and left the document those beats are
required to read first unswept. §6 of that audit says so in its own ground truth: *"The
canon file `docs/mechanisms-recommitted.md` is read by no test, but it cites cfd-203 **by
line number** at `:623` and `:694`."* This section is the record of sweeping it. The
method, the categories, the supersession form and the disposition vocabulary are §6's,
unchanged.

## Ground truth at the sweep

- Boards `C:\dev\skyrail-boards` at `86c871b` when the sweep started; `node --test
  test/*.test.js` re-run there by this synthesis: **tests 716, pass 716, fail 0**, 116.8 s.
  PWA `C:\dev\skyrail` at `840dbd0`, read-only reference for the lore quotations; its suite
  was never run, and no git write, dev server, browser or deploy touched either repository.
- **The tree moved under this synthesis, and the record says so rather than pretending it did
  not.** Three commits landed while the edit sets were being built — `079b2bd` (the hub
  index), `b232440` (the CFD-176 beat swept), and **`b95a9c9`, which completed `KILLS.md`**
  (67 to 119 lines) with the three sits the ledger was missing. HEAD reached `238aebe` by the time this
  closed — five commits on from where the sweep started, none of them this file. **The canon file is byte-identical across
  all of it**: `git rev-parse` gives blob `8696e6c3` at `86c871b`, at `b95a9c9` and in the
  working tree, so every anchor and every line number in this record and in the edit sets is
  still valid. What did move is covered under *Overtaken at review* and in the citation
  table — including four **new** inbound line-number cites into the canon that `b95a9c9`
  introduced.
- **Two figures in the dispatch brief do not reproduce, and both were caught by all eight
  sections independently.** The brief gave HEAD as `f3f6222` and the canon file as 758
  lines. HEAD is `86c871b`, one commit on (`f3f6222` is its parent, and
  `git diff --stat f3f6222 HEAD -- docs/mechanisms-recommitted.md` is empty, so every line
  number below holds at either tree). The file is **749 lines**, 43,024 bytes, 0 CR, 0 NUL.
  There is no line 750-758; the `s7.5` sweeper's range was 667-749, not 667-758.
- **No test reads this file.** `grep -rn "mechanisms-recommitted" test/ scripts/` returns one
  hit, a header comment at `test/dawnspur-dispatch.test.js:5`. Nothing here can go red, which
  is the reason the file drifted and the reason the sweep was worth its cost.
- **Governing status, measured, not assumed.** `grep -n "SIGNED\|RULED\|WRITTEN DOWN"` gives
  `:147`/`:149` §5 SIGNED (David, 2026-08-25) · `:229` §7 RULED (David, 2026-08-26) · `:360`
  §7.1 RULED (2026-08-27) · `:466` §7.2 RULED (2026-08-27) · `:492` §7.3 RULED (2026-08-28) ·
  `:551` §7.4 **WRITTEN DOWN** (2026-08-31, *not* ruled) · `:667` §7.5 RULED (2026-09-01).
  **§§1-4 and §6 carry no marker at all** — `git blame -L 1,92` gives 92/92 lines to
  `a8a54f05`, and a span diff of §2-§5 against `a8a54f05` returns exactly one changed line
  (§5's heading). The `s7.2-7.3` sweeper's row S7.2-M2 asserted §7 was "not one of the
  RULED/SIGNED sections"; its refuter corrected that on the tree, and the correction is the
  reason that row's ground was rewritten before it was built.
- **The `s7.4` status is itself contested and is a David call.** The heading says WRITTEN
  DOWN; `docs/cfd-201-beat.md:750`, landed by the 2026-09-01 sweep, says "canon §7.4, RULED
  2026-08-31". That is the corpus's only status word for §7.4 against twelve bare "canon
  §7.4" cites in cfd-205/208/209/210. Linear is unreachable from here — prefer null. *(Superseded in place 2026-09-09: the beat clause this row cites no longer says it. The cite is `docs/cfd-201-beat.md:753`, not `:750` — `:750` is a bare cite — and that clause was struck to a bare "canon §7.4 refuses" by counsel on David's instruction, 2026-09-09; the beat's own note at `:755` records what it replaced. The interim ruling, the two near-misses and the measured census are in `docs/decisions-open-2026-09-02.md` entry 5, which stays open pending David's yes/no. This row's "twelve bare cites" figure is corrected there, not here.)*
- One held artefact from the 2026-09-01 city synthesis targets this file and was not
  rebuilt: `C:\tmp\sweep\edits-city\mechanisms-recommitted.cfd-210-M4.DAVID.json`. See
  *Dispositions*.

## Method

Eight sweepers and eight refuters, sixteen agents, read-only, sharing the live tree; every
behavioural claim driven from `createBoard({fresh:true})` (or the board's own opening where
that board takes no `fresh` flag — `/dawnspur-dispatch/` and `/dawnspur-storm/` do not) through
the board's own commit functions. Where a claim was about a killed build, the killed bytes
were extracted by `git show` and their sha re-checked against `KILLS.md` before driving them.
The synthesis then kept CONFIRMED and PARTIAL rows whose refuter agreed the action, dropped
disputed-action rows into *do not average*, deduplicated across sections, built one anchored
edit set (anchors **cut** from the file by line range, never retyped, refused unless unique
and non-overlapping), and put rows only the refuter proposed into a separately labelled set.

**Three things this sweep did that §6's did not, all forced by the subject matter.**

1. **Line-number self-citations were stripped from every replacement.** The parentheticals
   naturally want to say "§7 at `:248`". Eighteen such cites were written and then removed in
   favour of section names and quoted phrases, because the edit set moves the file by 154
   lines and every one of them would have been wrong on landing — the same defect the sweep
   is correcting, manufactured by the correction. What remains are cites into *other* files,
   which do not move. This is `CLAUDE.md`'s "do not re-type a number into this file" applied
   to a file no test grades.
2. **Cross-section dedup was load-bearing, not bookkeeping.** Three findings were reached
   twice from different sections, and one pair would have contradicted each other if both had
   landed. See *Dispositions*.
3. **Every replacement's own quotations were graded by the standard the sweep is enforcing.**
   Two of them failed it and were fixed before the dry run: the Core Loop sentence proposed as
   the fix for `:195` and the one proposed for `:180` were both prefix quotes closed without an
   ellipsis — the defect of findings 5 and 10, reproduced inside their own corrections. Both
   now carry the ellipsis. This is the check `s7.5`'s refuter caught its sweeper failing inside
   the row about prefix quoting, and it is worth running on any edit set this file produces.

## Rows by section

| section | lines | checked | holds | mismatches | uncheckable | rows | refuter missed | C / P / R |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| §preamble + §1 | 1-92 | 41 | 38 | 2 | 1 | 2 | 5 | 1 / 1 / 0 |
| §2 · §3 · §4 | 93-146 | 27 | 21 | 4 | 2 | 4 | 3 | 1 / 3 / 0 |
| §5 · §6 | 147-226 | 29 | 22 | 6 | 1 | 6 | 5 | 4 / 2 / 0 |
| §7 body | 227-359 | 38 | 24 | 10 | 4 | 14 | 5 | 13 / 1 / 0 |
| §7.1 | 360-465 | 31 | 23 | 6 | 2 | 6 | 5 | 4 / 2 / 0 |
| §7.2 · §7.3 | 466-550 | 22 | 20 | 2 | 0 | 2 | 1 | 0 / 2 / 0 |
| §7.4 | 551-666 | 40 | 22 | 7 | 12 | 7 | 4 | 5 / 2 / 0 |
| §7.5 | 667-749 | 25 | 20 | 4 | 1 | 5 | 5 | 2 / 3 / 0 |
| **total** | **749** | **253** | **190** | **41** | **23** | **46** | **33** | **30 / 16 / 0** |

79 rows. **Not one sweeper row was REFUTED outright**, which is unlike the beat sweep and is
explained by what this file is: prose with no test behind it, where almost every claim is
either a quotation that can be diffed against a source or a date that can be diffed against
`git log`. What the refuters did instead was harder to see and more valuable — on **eleven**
rows they confirmed the defect and refuted the sweeper's *diagnosis*, its *ground*, or its
*replacement figure*. Five of those would have put a new false statement into the file.

## Counts

**Category** (80 assignments over 79 rows; `s7.4-M1` is both STALE and WRONG):
STALE 24 · WRONG 23 · INCONSISTENT 14 · UNCITED 10 · UNCHECKABLE 4 · MECHANISM 1 ·
DRIFTED-CITE 1 · UNSUPPORTED / UNREPRODUCIBLE / UNDER-SPECIFIED 3.

**Severity:** HIGH 3 · MEDIUM 29 · LOW 47.

**Action:** SUPERSEDE built **51** (26 sweeper rows in the agreed set, 3 refuter-missed rows
folded into agreed rows that touch the same sentence, 20 refuter-missed rows in the
refuter-only set, 2 sweeper rows built on the refuter's override of a NONE) · DAVID **13** ·
NONE **10** · disputed-action, not built **3** · **dropped by dedup 1** · **withdrawn as
overtaken by a concurrent landing 1**.

The three HIGH rows are `§7-main M1` (refuter-only), `S7-09`, and `S71-M3` (David's).

## The findings, ranked

**1. HIGH — `:248`, and it is the sweep's best row, found by a refuter.** The sentence one
line under the pass table is a present-tense universal over every passed board, and it is
false at four of the nine.

> Canon `:248`: *"Every passed board carries three or four levers."*
>
> Measured, by driving `createBoard({fresh:true})` on each of the nine boards passed at HEAD
> and recording every verb that ever fires — the instrument that reproduces this table's own
> verb column exactly (`commit*` and `can*` both return 4 / 4 / 3 for heat / scale /
> dispatch): heat 4, scale 4, dispatch 3, `/dawnspur-halt/` 4, `/dice-at-the-places/` 3, but
> **`/dawnspur-line/` 5, `/mosswake-loop/` 2, `/herbs-larder/` 1, `/they-remember/` 1**.
> Robust to whether the `commitPosted` dispatcher is counted (5 / 5 / 2 / 2).

All four outliers passed *under this amendment*. The sweeper graded the table's rows and its
header and not the sentence they exist to support. Built refuter-only; what it means for the
argument beneath it is left open rather than resolved.

**2. HIGH — `:336-338`, "The first cumulative board" names the wrong board.**

> Canon `:336-338`: *"**CFD-201, the storm sitting**, reframed by this ruling:
> `/dawnspur-dispatch/`'s loop and `/dawnspur-scale/`'s ground on one town, with **weather as
> the single new system**."*
>
> §7.1 `:394-396`: *"Ruled: **the provisions join ships and weather defers.**"* ·
> `public/index.html:81` names `/dawnspur-line/` *"the first cumulative board"* ·
> `docs/cfd-201-beat.md:68-76`: the storm *"shipped at `/dawnspur-storm/` (`42e4aa8`,
> 2026-08-28) … it stopped after five recuts and no pass"*.

§7 carries no forward pointer to §7.1 anywhere in its body (`grep -nE "§7\.[0-9]"` over
227-359 returns nothing), so a canon-first reader — which is every beat author, by this
file's own `:220-221` — meets the wrong board and never learns otherwise. Built.

**3. HIGH — `:427`, item 4's premise, and a DAVID call rather than an edit.**

> Canon `:427-429`: *"It signs *after* the storm sitting, so the last passed board at its
> signature is the storm board. Re-basing it today is precisely \"assumed at drafting,\" which
> this item's own clause forbids."*
>
> `sit/dawnspur-storm/MANIFEST.txt:2-6` carries RECUT 1-5 · canon §7.4 `:590` *"stopped at
> five"* · `grep -n -i storm KILLS.md` returns one line, `:49`, inside the scale kill — **no
> storm kill line**. Neither passed nor killed.

The sweeper wrote that the rule now resolves to `/dawnspur-line/`. Its refuter **refuted that
on the tree**: five city boards have passed since, and the last passed board at HEAD is
`/dice-at-the-places/` (`docs/cfd-209-beat.md:6`, PASSED 2026-09-01). That correction changes
the disposition rather than decorating it — on the sweeper's version the withdrawn re-base
quietly re-lands, and on the measured version the rule points across the desk/city seam into a
lineage no document here reconciles. Not built. See *David's calls*.

**4. MEDIUM — `:71`, R8 quotes the World Bible in words the World Bible does not use.**

> Canon `:71-72`: *"\"Three distinct terms hold: liftstone (material), keel stone (apparatus),
> and keel-fire (warmth).\" (World Bible, resolved)"*
>
> `Skyrail-Reclamation-World-Bible-v0.1.md:464`: *"…**Hold three distinct terms**: liftstone
> (material), keel stone (apparatus), and keel-fire (warmth)."*

A case-insensitive search of the whole lore tree returns **zero** hits for the canon's wording
and exactly one for the source's. The refuter added the half that will otherwise get the fix
undone: there are two World Bibles in the PWA tree and they disagree on this item —
the top-level `.extracted.md:462` is the earlier, *unresolved* cut (*"Confirm 'liftstone'
(recommended) versus 'keelstone'"*), and only the `weekend-worldbuilding-2026-06-29/source/`
copy carries the resolution the attribution silently relies on. Built with the file named.

**5. MEDIUM — `:190`, the Economy quotation does not exist.**

> Canon `:190-192`: *"\"Raiding and loss are a soft sink, and it earns its place as drama and
> texture rather than as the economy's regulator. The regulator is the cold.\" (Economy)"*
>
> `How-the-Economy-Works.md:45` is the section **heading**: *"## Raiding and loss are a soft
> sink, not the regulator"*. The body sentence is `:47`: *"**This is** a soft sink, and it
> earns its place…"*

The first six words are the heading with *", not the regulator"* dropped, welded onto a
different line. No line of the corpus carries the joined sentence. **The same splice has
already propagated into a signed beat** at `docs/cfd-203-beat.md:259` — outside this glob,
routed not edited. Built.

**6. MEDIUM — `:199`, the evidence for a signed item was wrong on its date.**

> Canon `:199-200`: *"one system per sitting has produced three passed sits"*
>
> `git log -S` puts the line's origin at `db8a02e`, 2026-08-25 17:22:45; the signature is
> `7081028`, 17:34:58. At that instant **one** sit had passed — the heat sitting, per PWA
> `c29090a` 13:36:36, *"the first sitting to survive a sit"*. The scale first cut had been
> killed that afternoon and its recut landed at 18:16:07; dispatch passed the next day.

The sweeper's replacement then froze the number at three; its refuter refused that too,
because the count has since been overtaken in the other direction. The built text points at
the tabulation instead of restating a number, and carries the second defect in the same
sentence that the sweeper missed: §5.6 is cited as a rule by two beats
(`cfd-183-beat.md:254`, `cfd-196-beat.md:417`) and §7 amended it 49 lines later with nothing
in between.

**7. MEDIUM — `:244-246`, the state-field column reproduces under no instrument.** Nine
candidate instruments were run over all three boards. `commit*` and `can*` both give the verb
column exactly (4 / 4 / 3). Nothing gives 4 / 7 / 8: fresh-literal keys and distinct
`s.<field>` references both give **8 / 7 / 10** and agree with each other; getters give
8 / 10 / 13; own keys 18 / 20 / 22. All three sims are byte-identical between the count commit
`d7cd1ab` and HEAD, so nothing drifted — the column was wrong on the day. The sweeper proposed
a note under a table whose cells still read 4 / 7 / 8; the refuter corrected the cells, which
is what §6 item 4 of this same file requires ("this file changes in place").

**8. MEDIUM — `:265`, the clearest measurement this project claims to have.**

> Canon `:265-266`: *"Same board, same day, same player. The only variable was **how many
> levers were in play**."*
>
> `ed7f49d` 12:58 → `2a3e9dd` 15:47 (Amendment 1) → `663d4fa` 16:30 (Amendment 2), all
> 2026-08-26 · `docs/cfd-196-beat.md:871-874`: *"The fix was the container, not the
> affordability — Amendment 2's placement."*

Same day and same player hold. Same board does not, and the passed beat's own reading of the
pass names the control's placement, not lever-count. `:266` calls this "the clearest
measurement this project has". Built.

**9. MEDIUM — `:553`, the rule's own headline claim, stale in ninety minutes and false when
written.**

> Canon `:553`: *"It produced **the only recut-to-pass in the corpus**"*
>
> `4fbb81f` 2026-08-31 12:12:27 wrote it; `a525218` 13:42:56 the same day is
> *"feat(lineage): /they-remember/ PASSES"*. And on the plain reading it was already false:
> §7's own table at `:244-245` lists heat and scale among the passed boards, and both are
> recuts of killed cuts (`KILLS.md:62`; `public/index.html:93` *"Recut after the first cut was
> killed"*).

`docs/cfd-208-beat.md:401-403` has already superseded its own copy of this sentence. The
canon's copy is the one still governing. Built.

**10. MEDIUM — `:625-628`, the quotation §7.4 blesses is not the quotation David wrote.**

> Canon `:625-628` closes: *"…TEND has become a habit carried from a board that no longer
> **exists.**\"*
>
> `docs/cfd-203-beat.md:1386-1390` continues: *"…no longer exists, **and it should either be
> cut or merged into the food line before Rustfall.**\"*

Two departures: a bold added on *"can't say what it bought them"* that the source does not
carry, and the sentence closed one clause short with no ellipsis. The omitted clause is the
sole source of §7.5's third routing cell, *"cut it, or merge it into the food line"* — and
§7.5 `:694` calls this quotation *"in full"*. Built; and see the dedup note below, because
this one edit resolves a second row in another section.

**11. MEDIUM — `:603`, "two intent questions" counts a question this section blesses.**

> Canon `:603-605`: *"it was **already tried** on `docs/cfd-201-beat.md`, whose *\"What the sit
> must report\"* asks two intent questions."*
>
> At signature (`30ff642`) that card held exactly two: David's TEND question — word for word
> the text this section holds up 20 lines later as *"look[ing] like an intent question and
> [not being] one"* — and the trim question in its intent form. Since `16ac8e5` the trim
> question has been recut into §7.4's own prescribed shape, so the card now asks none.

The audit already recorded this as an out-of-scope inconsistency inside the canon
(`:1025-1027`, under *do not average*). It is in scope now. Built.

**12. MEDIUM — `:327`, the gate that refuses an uncited beat points at a section that does not
exist.** `grep -n "§6\.\|6\.1"` over the file returns exactly one line — `:327` itself. §6 has
four numbered items and no subsections. Given the canon-check gate, this is the one
unresolvable pointer in a file whose job is to send beat authors somewhere. Built
refuter-only.

**The rest, by severity.** MEDIUM, built: `:110` and `:126` (two stale referents in §3 and §4,
both true when written and both describing a path or a task that changed within the hour);
`:240` (the pass table is cited by the audit as where passes live and is six boards short);
`:260` (a Linear-only quote transcribed two ways in one landing, whose lever count the passed
beat contradicts); `:308`, `:312` (two costs discharged without a marker); `:457`, `:459`
(the correction's own record — the refutation was never a thousand lines in, and there were
three copies of the withdrawn sentence, not two, the third governing unmarked for four days);
`:418-422` (the same undercount at a second line, plus a misdate: the refuting paragraph was
`c4af5a5` seventeen minutes after canon, not `f4c7b8b` three and a half hours after — the item
contradicts itself about what `f4c7b8b` did); `:111-113` (driven: the convicted `wait()` takes
nothing at a held pad, so *"decay-on-idle at a held greenhouse"* is measurably false of the
build it convicts, and the accurate mechanism is R3's treadmill verbatim); `:258` (MEET is
mandatory, driven, so "one lever engaged (SEND only)" cannot be right); `:357` (the premise
"by then the town has weather" against this section's own inheritance rule); `:78` (R9's
heading cites directive 1.19 for a three-way distinction 1.19 does not make — 1.19 is a
two-pole dyad and contains no "Growth"; the three lanes are 5.9). LOW, built: `:3`, `:22`,
`:81`, `:178`, `:180`, `:195`, `:203`, `:211`, `:221`, `:306`, `:394`, `:404`, `:408`,
`:454`, `:505`, `:569`, `:596`, `:664`, `:683`, `:727`, `:737`.

## Beat-by-line-number citations

The canon cites a beat by line number in exactly two places, and both are the same cite.
Both were re-pointed twice on 2026-09-01 (`:1352` → `:1379` at `16ac8e5`, `:1379` → `:1386` at
`5d1fdde`) and both were checked here by two sections independently.

| canon line | cite | quoted text at HEAD | drifted? |
| --- | --- | --- | --- |
| `:623` | `docs/cfd-203-beat.md:1386` | *"Ask the sitting to report **whether anyone tended at all, and why.**"* | **no** — line 1386 still opens the block quote |
| `:694` | `docs/cfd-203-beat.md:1386` | same passage | **no** |

`:623`'s cite is correct as a start-line cite; what is wrong is the *quotation beneath it*,
which is finding 10 and is built. `:694`'s cite is correct and its claim *"Quoted in full at
**§7.4**"* is false only until finding 10 lands.

**Inbound line-number citations into the canon drift on landing, and must be re-pointed in the
same commit** — the discipline `16ac8e5` and `5d1fdde` already used in the other direction:

| citer | cite | HEAD | after agreed | after both |
| --- | --- | --- | --- | --- |
| `name-collisions-audit…:874` | canon §7:240-246 | 240 | **276** | **319** |
| `name-collisions-audit…:838` | canon `:589-590` | 589 | **730** | **816** |
| `name-collisions-audit…:1025` | canon `:593` | 593 | **734** | **820** |
| `name-collisions-audit…:1033` | canon `:623` | 623 | **769** | **861** |
| `name-collisions-audit…:1033` | canon `:694` | 694 | **847** | **940** |
| `KILLS.md:60` **(new at `b95a9c9`)** | canon `:572` | 572 | **698** | **784** |
| `KILLS.md:62` **(new at `b95a9c9`)** | canon `:589` | 589 | **730** | **816** |
| `KILLS.md:87` **(new at `b95a9c9`)** | canon `:591-592` | 592 | **733** | **819** |
| `KILLS.md:89` **(new at `b95a9c9`)** | canon `:590-591` | 590 | **731** | **817** |

**Nine, not five, and four of them are younger than this synthesis.** `b95a9c9` added four
line-number cites into this file while the edit set that moves those lines was being built.
That is the serialised-landing problem in miniature, and it is why the re-pointing belongs in
the landing commit and not after it. Note also what `KILLS.md:60` points at: canon `:572`,
*"1. grey-square random clicks — **fail**"* — the list item finding **s7.4-M2** corrects,
because that sit was the site's and not the halt's. The ledger's new entry says the same
thing in its own words (*"2026-08-30 · Dawnspur Site · recut 2"*), so the two documents
already agree on the fact and disagree only on which is quoted as the authority. Land them
consistently.

No beat file cites the canon by line number — `cfd-203:177`, `:184`, `:1571` and
`cfd-210:333` cite §7.1.4 by section, which does not move.

## Refuted at review — do not re-find

- **`s1-R10`, "R10's Core Loop quote has two unmarked elisions."** The fact reproduces; the
  *ground* does not. The sweeper wrote that "every other multi-clause quote in §1 marks its
  cuts with an ellipsis". Extracting all 26 quoted spans in lines 1-92 and matching them
  against the corpus: **nine** quotes close with a period where the source continues
  mid-sentence (`:19`, `:38`, `:43`, `:50`, `:54`, `:58`, `:66`, `:74`, `:85`), two of them
  cutting mid-clause exactly as R10 does. None distorts. The section already contains the
  correct model twice (`:21-22`, `:72-73` end mid-sentence quotes with no terminal period).
  Superseding one of nine manufactures the inconsistency it claims to cure.
- **`S7-10`, "the convoy defense instance" as a stale name.** Refuted on shipped bytes:
  `grep -rn "convoy defense"` returns the identical route note in
  `public/dawnspur-dispatch/sim.js:154`, `public/dawnspur-line/sim.js:114` and
  `public/dawnspur-storm/sim.js:161`, plus three beats and the PWA handoff. `cfd-200:153`
  refuses `/convoy-defense/` as a *board path*, not as a description. The sweeper's own
  evidence concedes the term "is not wrong as a description". The real defect on that line is
  its premise, built separately.
- **`7.5-M1`'s account of `s5-M1`-style misquotation, inverted.** `:178`'s clause is **not** a
  misquote of Two-Games — it is a *verbatim* quote of the Master Design Bible, directive 1.12
  (`Master-Design-Bible.md:41`), under a Two-Games attribution. A string search of all 66
  files in the lore source tree finds it exactly once, in the MDB, never in `The-Two-Games.md`.
  The sweeper's fix would have overwritten an accurate quote with a different source's wording
  and recorded a misquote that never happened. Built as a **mis-citation**, not a misquote.
- **The `s7.5` sweeper's reading of "(d)".** It wrote that `:158`'s unlettered "(d)" "can only
  mean 'none this sitting'". Refuted by the signed text two lines above its own citation and
  by the board: driven from `createBoard({fresh:true})`, five CARRYs take the reserve
  4→3→2→1→0 floored, TEND is marks −1 / reserve +1, and level never falls across 50 carries —
  the recut ships the reserve, so "(d)" is the non-lethal half of (c), not none. NONE still
  stands, on the different ground that `:149-153` states the ruling in words.
- **`S7.2-M2`'s ground.** "§7 is PM prose, not one of the RULED/SIGNED sections" is false —
  `:229` reads **RULED — David, 2026-08-26**. The action survives on the narrower true ground
  (David's words end at `:236`; the cost list is the author's drafting; the retirement is
  David's own later §7.2 ruling), and that is the ground the built row carries.
- **Four sweeper figures that did not reproduce**, corrected before landing: 40 headings in
  `cfd-201-beat.md` (it is 34) and "the file is `:1-1596`" (1,610 lines; `:1596` is the last
  heading); "the beat is 1,553 lines at HEAD" (that is the count at `796d9a2`; at HEAD it is
  1,610 — a stale figure inside a row about a stale figure, and the built text drops the count
  rather than re-typing it); "§7.1 is 130 lines further down" (24 to the heading, 58 to the
  ruling); "false one hour later" (29 minutes to the draft, 58 to the ship); and
  `KILLS.md`'s "hosted-era holds the two halt sits only" (it holds five entries and nine
  `Recut:` lines, four of them without shas by the ledger's own heading — which is why that
  row corrects the word *every* instead of narrowing the pointer to `:26-39`, a fix that would
  have dropped seven recut decisions including the scale kill §7 itself discusses).
- **Two candidates default-refuted by their own refuters, recorded so nobody re-finds them.**
  R1's *"(World Bible carries the in-world tooltips)"* — `World-Bible-v0.1.md:427` carries all
  five verbatim; the doubting grep was case-sensitive. And R10's *"not four exclusive
  buttons"* — corroborated, not contradicted: `public/dawnspur-scale/sim.js` has exactly four
  commits, with the caveat that this is the sim at HEAD and not the killed `8f13c683` bytes.

## Not swept

- **Linear.** Twelve of the file's quotations have no in-repo source and are reported as
  unavailable, never graded: David's `:231-233` observation (`thiunk`/`beign` return only this
  file), the `:119` stakes sentence and the `:134` "scaled up and down … through the HUD"
  direction (**both** of which §3's and §4's arguments rest on, and neither of which exists
  anywhere in either repository), the `:347` weather-unit sentence, the `:260` sitting-2 quote,
  the four §7.4 ad-hoc refusals, the halt pass verbatim at `:583`, "no further grey-square
  recut", "why more glass", and the `:606` confabulated-mechanism sentence. **The `19:34`
  timestamp at `:119` cannot be 2026-08-25 local time** — the canon landed 17:18 −0400 and the
  beat that answers "the standing sentence" predates `5fabe7c` at 16:55 — so it is an earlier
  day or a UTC stamp; prefer null.
- **`skyrail-boards#2`**, a draft PR not present in this repository: any claim about what it
  implemented is null, and the word "refill" was dropped from the description of the killed
  board accordingly (the shipped kill has `tickLeak`/`tickDie` and no refill).
- **Whether CFD-145 as filed also rules sell-back.** `PLAN.md:1379` says the quote is
  "recorded on the issue"; no command here can see the issue.
- **The `:598` two-recut-cap denominator, "one case out of six."** The six recut sequences are
  enumerated nowhere and cannot be reconstructed from the repository — only `/dawnspur-scale/`
  declares a RECUT in a sim header, the storm carries five in its own, the site's two are named
  only in its beat, the dispatch's two shipped as "Amendments". A plausible six closes the
  arithmetic; it is not published as a measurement. The two claims beside it *do* reproduce.
- **`:641`'s "David asked for copy, verbatim, three sits running."** The three pure-copy recuts
  are confirmed (storm recuts 3, 4, 5; `cfd-201:72`). Of the three sit verbatims the repo
  carries, the third asks for text in terms and the fifth complains about descriptions, but the
  fourth — `sit/dawnspur-storm/sim.js:70`, *"The Ranger is on the roster. Storm sends offer the
  long way."* — is David naming copy that worked. Unsupported in-repo, not refuted.
- **`:727`'s "twice found the null to be the most informative outcome."** Measured: `grep -n -i
  "null\|nobody\|inconclusive" KILLS.md` returns **zero** hits, and neither registered null has
  fired — TEND's split was superseded by the cut before its sitting (`cfd-203:1392`) and
  CFD-210 is signed and unsat. The sweeper's "one instance is recoverable from the halt sit-3
  stop" is refuted: David *acted* in that sit and `KILLS.md:26-30` records a stop with a verbal
  report. The built text says "registered", which is true, rather than a count that is not.

## Overtaken at review

**One built row was withdrawn because another worker fixed the thing it described, in the
tree, while this synthesis ran.**

`s7.4-M6` (`:664-665`) charged that *"`KILLS.md`, where **every recut decision and its shas**
are recorded"* over-claims. At `86c871b` that was measured and true: nine `Recut:` lines, four
of them without shas by the ledger's own heading, and nothing at all for the storm's five
recuts, the site's sit, or the halt's pass. At `b95a9c9` the ledger carries all three — the
they-remember cut 0, the site fail, and the storm's five with `299c950` named and recut 5's
hashes re-measured — 119 lines and ten `Recut:` lines. **The row is withdrawn from the edit
set** rather than landed against a claim that has largely been discharged.

The residual, for whoever holds `KILLS.md` this wave and not for this edit set: the four
pre-host entries still carry recut decisions with no shas, by the ledger's own heading at its
own top, so canon's *"and its shas"* is still not literally true. That is a LOW note on a file
someone else is actively editing, and the ownership rule says leave it to them.

**Six `KILLS.md` line-number cites inside the replacements were de-lined for the same
reason.** The ledger grew by 52 lines during the run, so `KILLS.md:41`, `:41-49`, `:62`,
`:26-31`, `:33-39` and `:38` were all stale within the hour. They now name the entry by date
and board — *"`KILLS.md`'s 2026-08-25 scale entry"* — which does not move. Together with the
eighteen canon self-cites stripped under *Method*, **no replacement in either set now carries
a line number into a file this wave is editing.** That was luck the first time and a rule the
second.

## Do not average these

- **`s1-R10`** — sweeper SUPERSEDE, refuter NONE. The fact is agreed and only the remedy is
  disputed: nine identical cases or none, never one. Not built.
- **`S7-10`** — sweeper SUPERSEDE on the name, refuter REFUTED the name and proposed the
  premise instead. The premise row is built refuter-only; the name row is not built, and if
  the orchestrator wants the beat named on that line for navigation, that is an editorial
  addition and must not carry a *(Superseded)* marker.
- **`7.5-M3`** (the Related line) — sweeper DAVID, refuter NONE. The line makes no claim of its
  own; it becomes true the moment the worked-example table is fixed, and a note there while the
  table stands would manufacture the self-contradiction `:695-696` exists to prevent. Not
  built. **Do not spend a David call on it.**
- **`7.5-M4` and `7.5-M5`** — sweeper NONE, refuter SUPERSEDE, **built on the refuter's
  ruling** and flagged here because the disagreement is real. `:727`'s ambiguity sits in the
  section about ambiguity; `:683`'s "in a subordinate clause" is not a grammar slip but the
  premise for `:681`'s "filed it as a detail", and §7.4 introduces those words with
  **Crucially**, in bold.
- **`S2-M3`** (the collapse-table verdict "killed for exactly this") — both readers NONE, for
  different reasons; the refuter measured the mechanism the sweeper left as a reading (the
  killed board's leak is wall-clock driven at `public/dawnspur/index.html:220-224`, 1400 ms to
  dim and 1800 ms to dead — a standing bill on a clock, which is R7 verbatim). The rule cite is
  sound; the causal claim is the author's; the "His row" column is David's and is not ours.
- **`S4-M4`** (the "CFD-145's rule" label for no-refund) — NONE, on provenance rather than
  deference: `git show abf9ab8:docs/cfd-183-beat.md` line 45 already carried the label at
  16:32:24, forty-six minutes before the canon line. The canon inherited it; a change here
  would re-open a settled reading across six beat files.
- **`s5-M5`** (the §5 elision that drops "and upkeep contracts" where R10 keeps it) — NONE,
  settled by evidence neither reader had at first: the one implementer who acted on both
  elisions read them correctly and used each where it belonged, `cfd-183-beat.md:231` and
  `:237`.
- **`s7.4`'s status label** — see *David's calls*. It is not a disagreement between readers; it
  is a disagreement between the canon's own heading and a beat the last sweep landed.

## David's calls — listed, never built

Thirteen, plus one optional. Each line: what the canon says · what the source says · what
David would rule.

1. **`:12`** · *"David signs this doc before any recut implements it."* · §5 was signed
   (`:149`) and §7.1/7.2/7.3/7.5 ruled, but §§1-4 carry no signature, and the recuts the
   sentence gated have shipped (`KILLS.md:47-48`, `:62`) · Is the doc-wide undertaking
   discharged by §5 plus the ruled §7 sections, or do §§1-4 still need signing before the next
   recut? Rewriting it to match events would convert a promise he made into a description of a
   promise partly kept.
2. **`:158`** (optional) · *"Proposed answer: (d) now, (c) next"* · the question at `:155-156`
   letters only three options and "(d)" has no antecedent in the file; the shipped recut
   carries the reserve, so "(d)" cannot mean "none" · Does "(d)" mean "none of the three
   stakes", or "the reserve without the storm"? One line; §5 is SIGNED, so glossing it is his.
3. **`:401`** · *"the storm still carries the haul and in-progress stakes when it arrives"* ·
   the storm arrived and carried stake (a) only: UP is instant on that board
   (`sit/dawnspur-storm/sim.js:15-16`), the file holds no in-progress or suspended state at all
   (`grep` returns zero), and the mechanic that would have housed §5's stake (b) was cut before
   signature by a paragraph that names §5's stake (b) as what it was cutting
   (`cfd-201:1500-1508`) · Is §5's (b) still owed by a later sitting, discharged by the stop,
   or superseded? Half a SIGNED commitment went unmet on the board convened to meet it.

   *(2026-09-09, by counsel on David's instruction — entry 8 of `docs/decisions-open-2026-09-02.md`.
   This row omitted the two passages that answer its own question, and cited the cut paragraph
   at a stale line range, now corrected above. `docs/cfd-201-beat.md:295-299` refuses stake (b)
   by name and puts the reserve in its place — "(b) is refused this sitting, and named. This
   board's only in-progress object is an UP, which lands instantly on the passed board… The
   reserve **is** this board's (b)." — and `:1500-1508` carries the cut of the UP-form with its
   own re-entry condition at `:1507-1508`, "If the sit says the storm is too soft at the terrace,
   this is the first thing to add and it costs one guard." Both are present, byte-identical, in
   the document David signed 2026-08-28 (`30ff642`), at `:280-284` and `:1444-1452` of that
   revision. Ruled: superseded as to every sitting now scoped — nothing owed to any of them —
   conditional on a scope question (lineage-wide, or board-scoped) returned to David. A second
   live restatement of the §5 promise exists outside this repository at
   `C:/dev/skyrail/docs/handoff/2026-08-02-orchestrator-kickoff.md:1211-1214`; that file is
   outside this batch's glob (`docs/sweep-2026-09-02.md` only) and is not touched here — carried
   forward for whoever holds it.)*
4. **`:427`** · *"the last passed board at its signature is the storm board"* · the storm was
   sat five times and stopped, unpassed and unkilled (§7.4 `:590`; no `KILLS.md` line); "the
   last passed board" now resolves to `/dice-at-the-places/`, in another lineage · What is
   CFD-200's parent now, was "the last passed board" ever meant to reach across the desk/city
   seam, and can weather that never passed a sit be inherited under §7's own rule?
5. **`:444`** · *"If the storm board is **killed rather than passed**, CFD-200 re-bases onto
   whatever did pass — which would then be the join board. `docs/cfd-200-beat.md` already
   carries that contingency"* · the beat's trigger is *"If CFD-201 **does not pass its sit**"*
   (`cfd-200:121-123`) — it has fired; canon's has not; and the named consequent no longer
   follows either · Which wording governs, and what does it resolve to?
6. **`:481`** · *"The floor send being free and always lit is the shape this already takes on
   the dispatch board"* — under the heading that a quick path must be **honest** · free and
   always lit is confirmed by driving; but net marks per send put the free halt behind
   Mosswake at every roster (0W 6.800 vs 6.960 … 4W 8.240 vs 8.976), while `cfd-203:1238`
   applies §7.2's test *as EV* and passes the line board on it · What does "honest" test — not
   EV-dominated, or free/always-available/never-locking? §7.2 is RULED. (§7.3 four lines later
   forbids the single-number netting an EV-only test performs.)
7. **`:551`** · the heading says **WRITTEN DOWN 2026-08-31** · `docs/cfd-201-beat.md:750`,
   landed 2026-09-01, says **RULED 2026-08-31**; twelve other cites give §7.4 no status word ·
   Did he rule it? If yes the heading is the stale thing and every §7.4 supersession in this
   sweep becomes his to accept; if no, the beat line is the edit. *(Superseded in place 2026-09-09: the cite is `docs/cfd-201-beat.md:753`, not `:750`, and it no longer says **RULED 2026-08-31** — struck to a bare cite by counsel on David's instruction, 2026-09-09. The question below still stands and is returned to him as `docs/decisions-open-2026-09-02.md` entry 5.)*
8. **`:598`** · *"a two-recut cap … would bind in **one case out of six**"* · the six sequences
   are enumerated nowhere and cannot be reconstructed; the two claims beside it reproduce ·
   Name the six, or mark the figure unreproducible. Do not let a reconstruction stand in.
9. **`:606`** · the confabulated-mechanism verbatim · Linear-only; the closest in-repo record
   (`sit/dawnspur-storm/sim.js:52-53`, second sit) is a *different* sentence with the same
   content, and it too is an answer about **why he tended** — while the intent trim question
   got a clean factual answer (`sim.js:49-50`, *"TRIM landed (considered at each storm send and
   left)"*) · Which question produced it? If the TEND question did, then the shape §7.4 calls
   "the one that DOES work" produced the one confabulated answer it cites as the reason to
   refuse intent questions — and §7.5 (RULED) rests on that reading at `:681-686`.
10. **`:641`** · *"because David asked for copy, verbatim, three sits running"* · two of the
    three in-repo sit verbatims support it; the fourth sit's is David naming copy that worked ·
    Does the Linear verbatim for that sit carry a copy request? If not the clause narrows to
    two of three.
11. **`:708-720`** · Worked example 2 registers three nulls · `docs/cfd-210-beat.md:548-578`
    registers **four**, the first two in David's own words, since `9618352` · **This is the
    finding the held edit already carries — see *Dispositions*. Do not build a second version.**
12. **RULED and CARRIED OUT 2026-09-02 at `20bf043`**, bundled into decision 4 of
    `docs/decisions-open-2026-09-02.md`. The phrase is gone; the section now opens on the
    beat's own framing, *"none of these is a failure — each is an answer."* Left in place
    rather than deleted, per the supersession rule. Original finding follows.
    **`:710`** · *"Three ways a fork can produce **no decision**"* · neither the beat at HEAD
    nor the `6abe720` version the table paraphrases calls them that; `6abe720`'s null 3 says
    *"The decision happened but had no content"*, and the beat's framing is *"None of these is
    a failure. Each is an answer"* · Wrong on day one, not stale — inside the ruled table
    block, so his. Survives the held edit's minimal form untouched.
13. **RULED and CARRIED OUT 2026-09-02 at `20bf043`**, in the same bundle. Replaced by
    the beat's *"All four log as 'the sitting ended.'"*, and the section now names
    `pressOns` and shows it resolves none of the split. **Its two line cites below are
    stale by +13** — the CFD-210 self-pin at `c1c41df` inserted 13 lines at `:197` of that
    test file, so `:1019` and `:976` now read `:1032` and `:989`. Not re-pointed in the
    line numbers below, which are the sweep's record as taken; re-derive before citing.
    Original finding follows.
    **`:718`** · *"**All three would be logged as \"the player used the fork.\"**"* · the
    shipped board keeps a counter that separates them: driven from `createBoard()`, banking
    gives `pressOns: 0` and either press-on gives `1`, and the suite asserts it at
    `test/two-ways-from-here.test.js:1019` and `:976`; the beat's line is *"All four log as
    'the sitting ended.'"* · By §7.5's own third prohibition at `:744-746` — *"If a split can
    be resolved by a counter, it was not a split worth registering"* — the section's worked
    example fails the section's own test. Also day-one wrong. His.
14. **`:724`** · *"Write the outcomes and their meanings into the beat, **before signature**"* ·
    the ruling at the head of the section and David's own words in the `6a55533` memo say
    **before the sit**; CFD-210's governing registration was written after signature
    (`fdf0744` 11:46:54 → `9618352` 14:14:34) and satisfies the rule as ruled while violating
    the obligation as worded · Is the boundary the sit or the signature? Matching the ruling is
    the smaller change; keeping "before signature" is a new obligation and needs his word.

Also for him, from the audit's standing list rather than this sweep: whether the storm's
stop, the site's stop and the five undated passes belong in `KILLS.md` — this sweep added
three more reasons to ask (`:240`, `:664`, `:598` all assert or rely on a ledger completeness
that does not exist).

### Correction — the fourteen canon line cites, re-pointed (2026-09-09, by counsel on David's instruction)

*Entry 35 of `docs/decisions-open-2026-09-02.md`. This block targets the FIRST "David's calls —
listed, never built" heading above (the one this correction sits beneath, immediately followed
by `## Dispositions`) — the file carries two further headings of the identical wording, neither
of which is this block's target: the CFD-176 record's at `:966`, added by this same 2026-09-09
pass, and the DOM-namespace sweep's at `:1895` (appended 2026-09-08; it stood at `:1632` in the
file this pass opened on) *(corrected at the fix pass, 2026-09-09; this read "a second heading … at `:1632`",
which was true of the pre-edit file and stale the moment this block landed)*.
Re-derived read-only at boards `e226a9f` against `docs/mechanisms-recommitted.md`, 1066 lines,
blob `b240b01e`, last moved at `5eadc7a` *(corrected at the fix pass, 2026-09-09; this read
"blob unchanged since the canon edits landed at `b8d6da3`", which is false and contradicted this
block's own `20bf043` rows — measured, `git rev-parse <c>:docs/mechanisms-recommitted.md` returns
`442f11ea` at `b8d6da3` (902 lines), `ccceff8f` at `c32ff52`, `344bf117` at `20bf043` (933 lines),
`b240b01e` at `5eadc7a` and at HEAD (1066 lines): three changes after `b8d6da3`, which is exactly
why this table is re-derived and not copied)*. Each row: the sentence this list
already quotes, beside its line number in canon today, not instead of it — the number alone is
not what a builder needs.*

| this list's old cite | the quoted sentence | re-points to |
| --- | --- | --- |
| `:12` | "David signs this doc before any recut implements it." | `:12` — unchanged |
| `:158` | "Proposed answer: (d) now, (c) next" | `:170` |
| `:401` | "the storm still carries the haul and in-progress stakes when it arrives" | `:495` (§7.1 item 1) |
| `:427` | "the last passed board at its signature is the storm board" | `:530` |
| `:444` | "If the storm board is killed rather than passed, CFD-200 re-bases onto whatever did pass" | `:547` |
| `:481` | "The floor send being free and always lit is the shape this already takes on the dispatch board" | `:593` |
| `:551` | the §7.4 heading, "WRITTEN DOWN 2026-08-31" | `:666` |
| `:598` | "a two-recut cap … would bind in **one case out of six**" | `:740` |
| `:606` | the confabulated-mechanism verbatim, "I noticed it degrade …" | `:752` |
| `:641` | "because David asked for copy, verbatim, three sits running" | `:795` |
| `:708-720` | "Worked example 2" heading, three nulls | `:861` |
| `:710` | "Three ways a fork can produce **no decision**" | **SUPERSEDED at `20bf043`, not re-pointed** — the sentence no longer exists in canon; the section's own framing runs at `:863-865`, "none of these is a failure — each is an answer" |
| `:718` | *"All three would be logged as \"the player used the fork.\""* | **SUPERSEDED at `20bf043`, not re-pointed** — replaced by "All four log as 'the sitting ended.'" at `:874`; the correction parenthetical recording both supersessions runs `:886-895`, and the two retired sentences survive only as quotations at `:889-890` and `:891-892` |
| `:724` | "Write the outcomes and their meanings into the beat, **before signature**" | `:908` — **not `:877`, which is a blank line at HEAD** |

Items 12 and 13 of the list above already record their own `20bf043` landing; this table does
not re-open them, only corrects where a reader goes looking. Nothing in
`docs/mechanisms-recommitted.md` is touched by this block.

## Dispositions

**The edit sets are built and dry-run; nothing is written to either repository.** `git status
--porcelain` in `C:\dev\skyrail-boards` is empty and the canon file is 749 lines, unchanged.
The dry runs apply the anchors to an in-memory copy and report; they write only the JSON.
**No replacement carries a line-number reference into this file** — see *Method* item 1 — so
the edit set does not need to be re-pointed against itself; the only re-pointing owed is the
audit's five inbound cites.

- **`C:\tmp\sweepcanon\edits\mechanisms-recommitted.json` — 24 edits, 26 sweeper rows.**
  Anchors cut byte-exact from the canon file by line range, every one unique, none
  overlapping; the blob is unchanged from `86c871b` through `b95a9c9`, so the anchors are
  valid at either. Dry run 24 / 24; 749 → 902 lines, 43,024 → 54,482 bytes, 0 NUL, 0 CR.
  (`s7.4-M6` was withdrawn after its dry run — see *Overtaken at review*.)
- **`…\mechanisms-recommitted.refuter-only.json` — 20 edits, 22 rows** (20 refuter-missed, 2
  refuter-only set). Dry run 20 / 20; 749 → 851 lines, 0 NUL, 0 CR.
- **`…\mechanisms-recommitted.refuter-only.post-agreed.json` — 1 edit.** One refuter-only row
  **conflicts with an agreed row**: `S3-M1` (agreed, `:110-111`) and the merged
  `R-M1 + R-M2` (refuter-only, `:111-113`) share line 111 of one bullet. Its HEAD anchor
  returns 0 matches after the agreed set lands, so the re-cut is supplied ready, anchored at
  `:120-122` of the post-agreed text and dry-run clean. **Land agreed first, then this.**
  Combined: 24 + 20 apply clean in that order, 749 → 1,004 lines / 61,947 bytes, 0 NUL, 0 CR.
  The three findings in that bullet must land together or it ends up half-corrected — the
  agreed row alone fixes the path and leaves the mechanism and the second referent standing.

**Three findings were reached twice, and the dedup is not bookkeeping.**

1. **`:306` — the retired cost.** Found by §7-body (`S7-06`) and by §7.2-7.3 (`S7.2-M2`).
   Merged into one edit that keeps the original line wrapping (§7-body's version) and the
   scoping precision (§7.2's — §7.2 retires only "sits get longer", not the authoring cost in
   the rest of the item), on the ground §7.2's refuter established rather than the false one
   its sweeper wrote.
2. **`:625-628` and `:694` — and this pair would have contradicted each other.** §7.4's row
   makes the blessed quotation exact; §7.5's row `7.5-M1` adds a parenthetical to `:694`
   saying *"§7.4's copy stops one clause short"*. **If both landed, the second would be false
   the moment the first did.** `7.5-M1` is therefore **dropped by dedup**: the deeper fix
   restores David's words and makes `:694`'s existing claim true with no edit. §7.5's own
   refuter reached the same conclusion from the other side — *"the project's standard, set in
   `86c871b`, is that a prefix quote without an ellipsis gets made exact."* This is the row to
   look at first if only some of the set is landed.
3. **`:199` / §5.6, `:260`, `:569`** — three refuter-missed rows that touch the same sentence
   as an agreed row. Folded into the agreed replacements at each refuter's own request rather
   than landed twice; each is labelled in the JSON.

**Held for David, already built, not rebuilt.**
`C:\tmp\sweep\edits-city\mechanisms-recommitted.cfd-210-M4.DAVID.json` is the §7.5 worked-
example-2 edit from the 2026-09-01 city synthesis. **This sweep reached the same finding**
(row `7.5-M2`, CONFIRMED, action DAVID agreed) and did **not** build a second version. Two
corrections to the held file's replacement text, both measured here, that should travel with
it when he sees it:

- Its opening line asserts *"Four outcomes a fork can produce that **no completion count
  separates**."* **That is false on the shipped board** — `pressOns` separates them, and the
  suite asserts it (`test/two-ways-from-here.test.js:1019`). Use the beat's own line instead:
  *"All four log as \"the sitting ended.\""* Landing the strong claim would put a new false
  statement into a ruled section while correcting a stale one.
- Its closing line reads *"All four **would be** logged as…"*; the beat reads *"All four
  **log** as…"*. Match the beat.

And the held file's **minimal alternative** (append a note, leave `:708-720`) is **not
sufficient**: `:710` and `:718` are wrong independently of whether the registration is three
or four — both were wrong against `6abe720` too, i.e. on the day they were written. They are
David's calls 12 and 13 above and must travel in the same bundle.

**If the orchestrator lands only part of this.** The order that is safe: (1) the agreed set;
(2) the audit's five inbound line cites re-pointed in the same commit, from the drift table
above; (3) the refuter-only set with the one row re-cut. **Re-check `s7.4-M2` against
`KILLS.md` at landing time** — it is the one built row whose supporting facts a concurrent
worker is still moving. Landing `7.5-M1` is never correct —
it was dropped for cause. Landing the agreed `S3-M1` without the refuter-only bullet row
leaves `:112` naming "the beat" for a file that refuses exactly that at its own `:144`.

---

# The spec-vs-source sweep of the thirteenth beat — cfd-176, 2026-09-02

§6:864-865 recorded `/dawnspur-heat/` as the one board the 2026-09-01 sweep did not read
against its beat: *"`/dawnspur-heat/` cites no beat — its spec is not in this repository"*.
The spec arrived on main at `758a249` the next day, and `f3f6222` gave it a provenance
header. This section is the sweep that §6 was missing. Same method, same nine categories,
same `90488ff` supersession form; one sweeper and one refuter, read-only, sharing the live
tree, both driving the sim from `createBoard({fresh:true})` for every reachable-state claim.

## Ground truth at the sweep

- Boards `C:\dev\skyrail-boards` at **`86c871b`**, clean — **not `f3f6222` as the brief
  stated**, one commit ahead. `86c871b` is docs-only and
  `git diff f3f6222 86c871b -- docs/cfd-176-beat.md` is empty, so the beat is unchanged and
  every row grades at the stated tree. Suite 716 / 716 (refuter, 37.9 s);
  `test/dawnspur-heat.test.js` 21 / 21 re-run at synthesis (951 ms). PWA `C:\dev\skyrail`
  at `840dbd0`, read-only reference for lore.
- The beat: 62 lines, **3,392 bytes**, 0 NUL, 0 CR. The **governing text is lines 11-62**,
  the 54 lines below the orchestrator's provenance header at `:3-9`, which was not graded.
- The board it governs: `sit/dawnspur-heat/sim.js` (133 lines, blob
  `292d66454d826cb2…6de06b`), `sit/dawnspur-heat/index.html` (291 lines, blob
  `b5f7e14f4ed82a81…bba995`), `greenhouse.png` (`7fdf746844ec7efa…7af1e74f`) — all three
  re-derived exact against `sit/dawnspur-heat/MANIFEST.txt`, and byte-copied to
  `public/dawnspur-heat/`. **The sim carries no `Spec:` line**; its header at `:3-6` names
  "CFD-176 sitting" only, which is why §6 could not route it. Heat is **DESK** lineage
  (`test/lexicon-ledger.js:37`).
- **No test reads this beat.** `grep -rl 'cfd-176-beat' test/` returns zero files, and
  `test/dawnspur-heat.test.js`'s only `readFileSync` targets are `sit/` and `public/` paths
  (`:16-17`, `:53`, `:57`, `:66-67`). It regex-reads the *board* and phrase-echoes the Kill
  list in test titles. **Every supersession below is therefore free of a regex re-grade** —
  the 112-regex preview discipline of §6 has no work to do here, and the only guard on the
  beat's substance is the board test, 21 / 21.
- Provenance: `90af963`, author "Cursor Agent", 2026-08-25 13:54:46Z, the sole commit on
  `origin/cursor/cfd-176-beat-7ef8` above a 2026-08-24 base; merged `758a249` 2026-09-02.
  The header's "opened 2026-08-24" is **not verifiable in this repository** — the branch's
  only commit is 08-25 — and its "restates the Does below line for line" **overstates**:
  `sim.js:3-6` paraphrases (its *"Path to first WARM stays GOODS → B → HOLD → WARM"* is the
  phase order, and *"The step can go. The bank stays."* is Sees, not Does). Recorded, not
  graded — the header is the orchestrator's, added after the sweep's governing range.
- `KILLS.md` carries **no heat pass**. Its six `176`/`heat` hits (`:23`, `:28`, `:56`,
  `:59`, `:62-64`) are kills and the recut pointer only. The pass's record is Linear.

## The rows

| checked | holds | mismatches | uncheckable | rows | refuter missed | C / P / R | refuter agreed the action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 61 | 48 | 10 | 3 | 10 | 5 | 6 / 3 / 1 | 8 of 10 |

Categories over the 10 sweeper rows: INCONSISTENT 5 · STALE 2 · UNCITED 1 · UNTESTED_KILL 1
· UNCHECKABLE 1. Severity: MEDIUM 2 · LOW 8. **Action after refutation: SUPERSEDE 2 · DAVID
4 · NONE 4.** The five refuter-missed rows are all DAVID (MECHANISM 3 · INCONSISTENT 1 ·
CANON 1; MEDIUM 4 · LOW 1), so **the refuter-only edit set is empty** — see *Dispositions*.
Fifteen rows in all, nine of them David's.

The three uncheckable are the same shape §6 recorded: "173 stays Done" (Linear),
"`workflow_dispatch` is run" (CI), and "#2 is deployed" (GitHub). Reported unavailable, not
as passes.

**Every driven figure was re-derived at synthesis, independently of both readers**
(`C:\tmp\sweep176\synth\drive.js`): the API has **18 own properties** — `banked canB
canGoods canHold canWarm commitB commitGoods commitHold commitWarm haul hearth heldA litJobs
marks phase step wait warmed` — and `core`, `tickLeak`, `tickDie` are all `undefined`; BFS
over the five ops reaches **exactly 6 states**; `hearth` takes only `off` and `held`; `step`
only `off`, `out`, `gone`; `marks` reads 0, 1, 3, 2, 2, 2 along the path; `litJobs` reads
`[goods]`, `[B]`, `[hold]`, `[warm]`, `[]`, `[]`; and **exactly 1 of 24 orderings** of the
four commits reaches `banked` — `GOODS > B > HOLD > WARM`. Nothing either reader reported
failed to reproduce.

### The two built

- **MEDIUM, `cfd-176:11`** — Seat: *"Superheavy beat only. No implement. No deploy. No
  merge."* Four live prohibitions, all four discharged. The board landed twenty minutes
  after the beat commit and is on main at **`4c5e9ec`**, 2026-08-25 16:25:22Z; CI deploys
  the byte-copy, and I fetched it: `https://boards.skyrailreclamation.com/dawnspur-heat/sim.js`
  → **200, 3,050 bytes, sha256 `292d66454d826cb2…6de06b`**, byte-identical to the blob and to
  `MANIFEST:20`. The beat itself merged `758a249`. David sat it and it **passed** — canon §3
  `:116-118` (*"The heat sitting (`/dawnspur-heat/`, passed) … it stands as played"*), canon
  §7's passed-board table `:240-246` (*"the three boards that have passed a sit"*), hub
  `public/index.html:97` (*"live — passed its sit"*) and `:99` (*"the first sitting to
  survive a sit"*). **No primary pass record in this repository** — no `KILLS.md` line *(as of 2026-09-02; one was entered 2026-09-09 under counsel's ruling on entry 7, date still null)*, no
  verbatim, and no date anywhere (hub `:97` carries none where the scale row at `:91` carries
  `2026-08-25`), so the note publishes **date null** rather than a plausible one. The
  provenance header records the *file's* merge and never that the board shipped or passed.
  Built in the `cfd-201:68-78` form, keeping `:15`'s protocol closer.
  **The refuter's material correction is in the built text:** `a030c55` — which the sweeper
  named as the landing — **is on no main ancestry** (`git merge-base --is-ancestor a030c55
  main` → NO; `git log -- sit/dawnspur-heat` does not list it), it is the Grok base cut on
  `grok/cfd-176-keel-fire-outward-step`. Citing it as a landed commit inside the sentence
  that condemns #2 for being off-main would have repeated the defect the row fixes. The
  built text names `4c5e9ec` as the landing and `a030c55` as the off-main base, and adds
  `db1a9ad`, `c887359`/`efbed23` and `7b3ddb6`, because the board at HEAD is their sum.
- **MEDIUM, `cfd-176:45`** — *"| Nouns (174) | **REFUSED** |"* on a shipped, passed,
  hash-pinned board that **carries the CFD-174 nouns**. `db1a9ad`, 2026-08-25 17:42:18Z,
  three hours 47 minutes after the beat: Dawnspur Halt, Greenhouse District, Mosswake Loop,
  CARRY FOOD, MOSSWAKE +3, REPAIR 1, WARM THE TERRACE, `String(board.marks) + " marks"`.
  `test/dawnspur-heat.test.js:284-308` asserts all seven present **and six placeholders
  absent by name** (`/GOODS A/`, `/B \+3/`, `/HOLD 1/`, `/aria-label="A"/`,
  `/aria-label="B"/`, `/>WARM</`). A reader of the row would expect the placeholder
  vocabulary the board's own test forbids. The beat's Does and Kill keep that vocabulary
  correctly — GOODS / B / HOLD / WARM are the sim's exported names — so **the row alone is
  stale** and nothing else in the file moves. Built, with the played numbers named as
  unchanged. The refuter's non-material note travels with it: `MOSSWAKE +3` is an *added*
  line in that diff (`sendB` was introduced there), not a rename of `B +3`; the absence of
  placeholders is carried by the test, not by a removal.

### Refuted at review — do not re-find

- **`cfd-176:13`, the live kill, graded UNCHECKABLE by the sweeper — it is checkable and it
  passes.** The sweeper read `kills/README.md:36-37`'s beacon caveat as unconditional and
  stopped at the sim half. **I reproduced the refutation myself:** a plain fetch of
  `https://boards.skyrailreclamation.com/dawnspur/` returns **200, 11,665 bytes, sha256
  `bdde9b50331ac89d…97ac59`** — exactly beat `:13`, the HEAD blob, `test:40` and
  `kills/README.md:31`. With `Accept: text/html` it returns **12,024 / `540c9ee2…`**;
  **12,024 − 11,665 = 359**, reproducing the README's own figure. The injection is triggered
  by the **Accept header, not the User-Agent** (the refuter isolated all four combinations).
  The sim half re-fetched at synthesis: **3,673 bytes, `395c18f28d5e04b5…c320c`** = the HEAD
  blob. **Beat `:13` and Kill `:61` are verified end to end on both halves.** Reusable
  method, and it removes a standing excuse: *to hash-match archived HTML at the durable
  host, fetch without an `Accept: text/html` header.*
- The furnace ban as an argument against `cfd-176:41`. `test:236` builds its subject as
  `SIT_HTML + "\n" + SIT_SIM` — **board bytes only, no `docs/` path in the file**. The beat
  carries "furnace" at `:41` and `:47` with the suite green. Naming a refused mechanic in
  its own vocabulary is the form every REFUSED row uses (`:40` Fuel, `:44` Foundry, `:47`
  Furnace word, `:48` charge/leak/refill), so "core" at `:41` and `:54` is not a defect
  either.
- `cfd-176:19` *"The haul is heat."* as a collision worth a beat note. The beat's `haul` is
  R10's load; the board's `haul` is the Mosswake cargo slab set by `commitB` (`sim.js:52`,
  `:87`; `index.html:101`, `:206`) two commits before WARM and never touched by it. **The
  collision is inert** — `grep` of `sim.js` shows `haul` at `:52`, `:87`, `:112`, `:123`
  only, and nothing reads it but a CSS class toggle. A collision no decision reads cannot
  change a played number. The remedy is a ledger row (`test/lexicon-ledger.js:597` already
  lists it UNADJUDICATED, desk-shared), not a beat edit. NONE.
- `cfd-176:23` *"WARM is not the only job."* as worth an edit. **Record the reason
  precisely**, because the sweeper's "true only vacuously" understates it: across all 6
  reachable states the sentence is **false at state 4** — where `litJobs()` is exactly
  `["warm"]`, WARM literally the only lit job — and vacuous at 5 and 6, where nothing is
  lit. It is never substantively true. The file resolves it internally two sections away
  (Ends `:27` *"Sitting stops."*, Kill `:55`, and `test:203` which *"states the end state
  outright — the board goes dark"*), which is why no edit is warranted and the note belongs
  here.
- `:15` *"David sits first. Ask: What happened."* as stale on a passed board — it is the
  sit-protocol closer of every beat in `docs/`, exactly as §6 refuted for `cfd-205-halt:286`.
  It is kept verbatim by the `:11` supersession.

### Untested Kill lines

**10 of 11 fully graded; one partial; none by a beat-reading test.** Mapping, corrected by
the refuter where the sweeper was wrong: `:52` → `test:153`; `:53` → `:134`, `:142`; `:54` →
`:163`, `:224`; `:55` → `:193`, `:203`; `:56` → `:180`, `:174`, `:72-84`, `:276`; `:57` →
`:281`; `:58` → `:235-257`; **`:59` → `:237-252`, not `:241`** — the line names four things
(furnace word `/furnace/i` `:238`, fuel clock `/\bfuel\b/i` `:239`, radius
`/(?<!border-|border)radius/` `:240`, CFD-163 `:252`) and they are graded across the whole
banned array; `:60` → `:120`, `:111`, `:94`; `:61` → `:38-44`, `:86-92` (blob), with the live
host now fetched and matched on **both** halves.

`:62` — *"#2 is merged, deployed, or `workflow_dispatch` is run."* — is the partial. Its
**substance is graded**: `test:224-233` asserts `b.core`, `b.tickLeak` and `b.tickDie`
`undefined` before and after `commitWarm`, and `:244-245` ban `/tickLeak/` and `/tickDie/`
in source. Its **process clauses are graded by nothing** — the same shape §6 recorded for
mosswake 15 of 16 and herbs 14 of 16. `git merge-base --is-ancestor 30e92ee main` → NO
confirms the underlying fact, but a git fact no test asserts is not a graded kill. NONE.

### Bridges

**None.** Every cross-model token the sweeper logged carries its desk meaning on a desk
board; there is no seam here to cross. Recorded so the five are not re-found:

| beat:line | token | the beat's meaning | the governed board | class |
| --- | --- | --- | --- | --- |
| `:19` | `haul` | R10's load — heat, on the one dispatch | `s.haul`: a boolean set by `commitB` (`sim.js:52`), the Mosswake cargo slab on dest B (`index.html:101`, `:206`); never touched by WARM | same word, two referents, **inert** — nothing reads it but a class toggle. Ledger row, not a bridge. |
| `:19`, `:23` | `wait` | the world's turn after the shuttle returns home | `wait()` `sim.js:72-79` — latent behind `banked`, drives `step` out→gone and `phase` sent→sat; fired only by the page's 1500 ms idle timer (`index.html:218-220`) | DESK on both sides. **This board is the origin of the timer convicted at §1.3** — see David's call R2. |
| `:23` | `home` | the place the shuttle returns to before the wait | `#home` is the home *pad* (`index.html:43-46`, `:143`); `at === "home"` gates every job and the timer | a desk position, **not** the city's post-event LANDED / COME HOME class (ledger `:701`). Not the `halt`/`home` mirror of §1.6. |
| `:19` | `marks` | civic money that does not buy WARM | `get marks()` — the wallet: 0 fresh, +1 GOODS, −1+3 B, −1 HOLD, untouched by WARM (driven 0, 1, 3, 2, 2, 2); `canWarm` reads no `marks` (`test:142-151`) | the beat and the board agree exactly. NONE. |
| `:23` | `lit` / `litJobs` | which jobs are available now | `litJobs()` `sim.js:32-39`, in the board's own words | agree. NONE. |

### Not swept

Linear is unreachable — **David's verbatim for the heat pass**, its date, and "173 stays
Done" are all null here, not zero. GitHub and CI are unreachable, so "#2 is deployed" and
"`workflow_dispatch` is run" are unverified. The provenance header's "opened 2026-08-24" is
unverifiable in this repository. The board's `index.html` was not re-fetched from the heat
path at synthesis (only `sim.js` was); the `/dawnspur/` index **was**, and matched.

### Do not average these

- **`cfd-176:11`, the discharged Seat gate — the precedent splits.** §6's own disposition at
  `:1253-1254` ruled this exact class **DAVID** for `cfd-205-halt:33-34` (*"leave; status
  lives in the next Seat"*), and that line still reads *"Draft. Do not merge. Do not deploy.
  Do not `workflow_dispatch`. David sits first."*, unsuperseded *(as of 2026-09-02; superseded 2026-09-09 under counsel's ruling on entry 9 — the line now carries the rewrite form)*. The distinguishing features
  are real and measurable — `cfd-176:11` is labelled `Seat:`, is the file's **first
  governing line**, and governs a board that is live, byte-verified and passed, which is the
  `cfd-201:68-78` shape — and the built text is in that form. **But the split is thin, and
  it is David's to confirm, not the sweep's to assume.** Built and dry-run clean; hold it
  behind his word if he wants the convention uniform.
- **`cfd-176:35` Heat vs Air — canon grades this board's WARM two ways, eleven lines
  apart.** `docs/mechanisms-recommitted.md:105` convicts *"WARM as ground step that is also
  a dest job"* as a **TRUE collapse | R9**; `:117-118` says the board's step-onto-ground
  *"survive[s] R8/R9 readings"*. The beat's `:35` sides with `:118`, and the beat's own `:19`
  has one WARM doing both things R9 forbids. **This is not resolvable by measurement and
  must not be averaged.** It also constrains the M6 note below: adding "Heat vs Air — R9" as
  a clean citation would silently pick `:118` over `:105`.
- **`cfd-176:39` vs canon `:106`'s order.** Canon writes the lock as `GOODS/HOLD/B/WARM`;
  the board enforces `GOODS → B → HOLD → WARM` (`sim.js:4`, and 1 of 24 by BFS). **The slip
  is canon's, not the beat's**, and out of this sweep's glob — but any note citing `:106`
  must quote canon's order as canon's and flag it, or cite `:116-118` (which says only "a
  phase lock") and leave `:106` out. Do not let the beat inherit it.
- **`cfd-176:19` "GOODS / B / HOLD are not on the same row" and `:41` "does not die unless
  fed".** Sweeper SUPERSEDE, refuter DAVID on both. Both are ellipsis/scope readings, not
  measured errors: the board facts are confirmed either way, and resolving an ellipsis is a
  reading of authorial intent — the class §6 routed to David at `:1258`. **Not built.** Note
  for whichever way `:19` goes: `#pads` holds **four** buttons, not three — `index.html:157`
  `id="go"` / LEAVE is on the civic row too, and `test:259-274` never mentions it, so a
  replacement naming only GOODS / B / HOLD would need superseding again the first time
  someone counted the row.

## David's calls — listed, never built

*Added 2026-09-09, by counsel on David's instruction — entry 39 of
`docs/decisions-open-2026-09-02.md`. This section is the list the promise below already
announces and this record did not build. Copied verbatim from `C:\tmp\sweep176\results.json`
(24,592 bytes, mtime 2026-09-02 11:47 local, sha256
`e84f655a606e23440eb62f6549f388d4495acfca8e3c1a90dae3a877cae3212c`) — copied out of volatile
scratch to `C:/tmp/council/batches/b13/sweep176-results.json` before this section was built, so
the enumeration survives independently of the session-temp directory it was found in. Listing
is not ruling: each row stays David's until he answers it. None of the nine is known to have
been overtaken — `git log --oneline -- docs/cfd-176-beat.md` returns exactly `b232440`,
`f3f6222` and `90af963` at `e226a9f`, and `b232440`, the last commit to touch the file, moved
only `:11` and `:45` — M1 and M2, both SUPERSEDE, both `action_agreed: true`, both outside the
nine below. The nine map to **five** ledger entries, not six — 12, 13, 16, 20 and 29 — verified
against this ledger's own headings; a tenth call, `cfd-176-M1`'s own "PRECEDENT SPLIT for
David" (audit `:1253-1254` ruled the same discharged-gate class DAVID for
`cfd-205-halt:33-34`), is folded into ledger entry 9 and is not one of the nine. The ruling's
per-row stamp — "open as at `e226a9f`, 2026-09-09; `b232440` is the last commit to touch
`docs/cfd-176-beat.md` and moved only `:11` and `:45`" — is carried once here for all nine
rather than nine times, because it is identical on every row and the same ruling's "de-line per
entry 35" asks for fewer bare numbers in the rows, not more.*

*THE CANON LINE NUMBERS INSIDE ROWS 3, 4 AND 8 ARE THE SWEEP'S OWN, AND ARE STALE AT `e226a9f`
(added at the fix pass, 2026-09-09, after a critic caught them being carried bare and
present-tense inside the same commit whose entry-35 block exists to stop exactly this). They are
carried verbatim because the rows are a verbatim copy of `results.json`, and they resolved when
the sweep ran on 2026-09-02 against the then-749-line canon at `5d1fdde` — verified: at
`5d1fdde`, `:105` is the Heat-vs-Air row, `:106` the phase-lock row, and `:116-118` the
heat-sitting bullet, exactly the sentences those rows name. At `e226a9f` they are elsewhere, and
per entry 35's rule the sentence travels with the number:*

| the rows' cite (2026-09-02, `5d1fdde`) | the quoted sentence | at `e226a9f` |
| --- | --- | --- |
| `:105` (rows 4, 8) | "Heat vs Air: WARM as ground step that is also a dest job / TRUE collapse / R9" | `:111` |
| `:106` (row 3) | "Profit/tend/buildout → GOODS/HOLD/B/WARM phase lock / TRUE — the deepest one / R10" | `:112` |
| `:116-118` (row 3), `:117-118` (rows 4, 8) | the heat-sitting bullet, closing "Its bank-in-the-stone and step-onto-ground survive R8/R9 readings." | `:125-127`, that sentence at `:127` |

*Row 4's "eleven lines apart" resolves only against `:105` and the bullet's opening `:116` — the
row's own second cite, `:118`, is thirteen lines from `:105`; on the same pairing the gap is
**fourteen** at `e226a9f` (`:111` to `:125`). Nothing in the nine rows' substance moves — only
where a reader must look to check it.*

1. **`cfd-176-M3`** (`:19`, "GOODS / B / HOLD are not on the same row.") — sweeper action
   SUPERSEDE, refuter `action_proposed` DAVID, `action_agreed` **false**, verdict **PARTIAL**,
   severity LOW · *Layout half CONFIRMED: `#heat-row` holds `warmA` alone, `#pads` holds
   `goodsA, sendB, holdA, go`, `test:259-274` grades both halves. But the sentence is
   ambiguous, not false — the elided comparator is WARM, recoverable from the preceding
   sentence and from the beat's own `:38`, which makes the identical elliptical move.* → ledger
   entry 29.
2. **`cfd-176-M4`** (`:41`, the furnace/core REFUSED row) — sweeper SUPERSEDE, refuter
   `action_proposed` DAVID, `action_agreed` **false**, verdict **PARTIAL**, severity LOW ·
   *Mechanism CONFIRMED independently: 18 own properties, no `core`/`tickLeak`/`tickDie`; BFS 6
   states; no feed verb. The furnace-ban argument fails — `test:236` builds its subject as
   `SIT_HTML + SIT_SIM` only, board bytes, no `docs/` path; "core" names the refused object, the
   form every other REFUSED row uses.* → ledger entry 29.
3. **`cfd-176-M5`** (`:39`, the four-button-phase REFUSED row) — sweeper DAVID, refuter
   `action_proposed` DAVID, `action_agreed` **true**, verdict **CONFIRMED**, severity LOW · *All
   24 orderings enumerated: exactly 1 reaches banked, `GOODS>B>HOLD>WARM`. Canon `:106` writes
   the lock as `GOODS/HOLD/B/WARM`, not the board's order — quote canon's order as canon's and
   flag it, or cite `:116-118` only. The slip is canon's, not the beat's.* → ledger entry 13.
4. **`cfd-176-M6`** (`:17`, no Canon-check section) — sweeper DAVID, refuter `action_proposed`
   DAVID, `action_agreed` **true**, verdict **CONFIRMED**, severity LOW · *`grep` for an R1-R10
   citation or "Canon check" returns nothing; canon quotes byte-exact. Canon has ALREADY
   performed the R8/R9 reading at `:117-118` — cite that rather than re-deriving one. Caution:
   canon is not self-consistent — `:105` grades this board's WARM a TRUE R9 collapse while
   `:118` clears it, eleven lines apart; picking one is David's.* → ledger entry 20.
5. **`cfd-176-R1`** (`:23`, "A stays held.") — refuter-missed, action DAVID, category
   INCONSISTENT, severity MEDIUM · *`heldA` is false in 2 of the 6 reachable states, both on
   the only path to WARM; `index.html:201` renders `destA` as `thin` there. The beat's own
   `:37` keeps held/thin ON and calls it "visible as a held slab" — intended and on screen. A
   charitable reading is available; hence DAVID.* → ledger entry 16.
6. **`cfd-176-R2`** (`:19`, "After return, wait.") — DAVID, MECHANISM, MEDIUM · *No tap binding
   of the six at `:253-273` reaches `board.wait()`; it fires from a 1500 ms idle timer inside
   `paint()`. This board is the ORIGIN of the timer convicted at audit §1.3, and the beat does
   not say a timer.* → ledger entry 16.
7. **`cfd-176-R3`** (`:27`, "The outward step is what could die.") — DAVID, MECHANISM, MEDIUM ·
   *`sim.js:74` sets `step = "gone"` unconditionally inside `wait()`. Replayed over every
   reachable banked state: step can never remain "out". No branch, no input, no timer value
   changes it — the stake was asserted, not modelled.* → ledger entry 16.
8. **`cfd-176-R4`** (`:35`, the Heat vs Air lock) — DAVID, CANON, MEDIUM · *Canon `:105` grades
   this board's WARM a TRUE R9 collapse; `:117-118` says it survives R8/R9. The beat's own
   `:19` has one WARM doing both things R9 forbids. Not resolvable by measurement — a
   do-not-average row.* → ledger entry 12.
9. **`cfd-176-R5`** (`:19`, "One dispatch.") — DAVID, MECHANISM, LOW · *The path to WARM is
   four mandatory out-and-back trips; exactly 1 of 24 orderings reaches `banked`. The design
   reading ("one dispatch carries the heat") is natural; hence DAVID and LOW. Mirror of M5 seen
   from the Does side.* → ledger entry 16.

Both filings out of this beat's glob are unchanged from the *Dispositions* note below:
`kills/README.md:36-37`'s unconditional beacon caveat, and the audit's `:864-865` "cites no
beat" half, which remains true.

## Dispositions

- **Built, dry-run clean at `86c871b`, not landed:**
  `C:\tmp\sweep176\edits\cfd-176-beat.json` — **2 supersessions**, `cfd-176:11` and
  `cfd-176:45`. Both anchors **cut from the file by line number, never retyped**, each
  occurring exactly once (307 and 29 bytes), spans `[579,886]` and `[2700,2729]` — **no
  overlap**, the property `apply-edits.js` exists to check. Both `*(An earlier cut read
  "…")*` fragments verified present byte-for-byte **inside the anchor they supersede**.
  `node apply-edits.js docs/cfd-176-beat.md … --dry-run` → *"2 edit(s) would apply cleanly"*,
  exit 0, repo tree unchanged after. Applied to a scratch copy at
  `C:\tmp\sweep176\preview\cfd-176-beat.md`: 62 lines still, 3,392 → 5,027 bytes, **0 NUL, 0
  CR**. No test regex to re-grade — no test reads this beat. No board byte, no pin, no
  `sit/` or `public/` path touched. Docs-only.
- **Refuter-only edit set: empty, and deliberately so.**
  `C:\tmp\sweep176\edits\cfd-176-beat.refuter-only.json` is `[]`. The refuter found five
  rows the sweeper missed and **routed every one of them DAVID**, so there is no
  refuter-only supersession to land. The file is written and dry-run for the record:
  `apply-edits.js` **refuses it** — *"REFUSED (empty): no edits given — refusing rather than
  writing the file unchanged"*, exit 1. That refusal is the honest output; an empty set is
  not a clean apply.
- **David's calls: nine on the beat, two filings out of glob.** Listed separately, one line
  each. See *David's calls — listed, never built*, above (added 2026-09-09).
- **Filed, not built — outside this beat's glob:** `kills/README.md:36-37` states the beacon
  caveat unconditionally when it is Accept-header-conditional (measured above), which is what
  made the sweeper stop; and `docs/name-collisions-audit-2026-09-01.md:864-865` — the *"was
  not swept"* half is superseded by this record, while the *"cites no beat"* half **remains
  true**, since `sit/dawnspur-heat/sim.js` still has no `Spec:` line. Both are edits to
  files this task does not own.

---

# The DOM namespace sweep — 2026-09-08

Append-only, dated, superseded in place. This record holds the sweep the 2026-09-01
name-collision ledger (`docs/name-collisions-audit-2026-09-01.md`) said in its own §3.2 it
could not see: *"Control ids, `data-action` attributes, CSS class tokens, label text and the
DOM-to-method wiring, across 4,860 lines. Only spot-grepped."* Two collisions had been found
there anyway and adjudicated — §1.6 `home` and the C13 delta's `lit` — and the landed guard
carries three `DOM_ROWS` over that surface. This is the record of sweeping the rest of it.

**Produced by four adversarial pairs — one sweeper and one refuter per surface (ids, classes,
wiring, text), read-only, each driving every loadable sim from `createBoard({fresh:true})`
and re-deriving every count with its own instrument** — then synthesised by one worker from
the pairs' verbatim results (`C:\tmp\domsweep\results.json`, 247,681 bytes, four surfaces,
26 sweeper findings, 26 verdicts, 15 refuter-missed rows; written to file first and worked
from the file). **The guard rows landed at `925d23a` in `test/lexicon-ledger.js` `DOM_ROWS`**
(see *Dispositions*) *(superseded at landing, 2026-09-08; this read "Nothing here is landed. The
proposed guard rows are in C:\tmp\domsweep\proposed-dom-rows.js, and they grade green at HEAD by
the guard's own loop")*. What is David's is listed and unbuilt, except call 6, built at `925d23a` (below).

**Verified by this synthesis, not taken on the agents' word:** HEAD and the suite; the
sit/public byte identity for all thirteen boards; every board list in every proposed row,
re-derived at HEAD with `test/lexicon-derive.js domInventory()` over thirteen boards; the
`stake undefined` defect on `/dawnspur-line/` (driven, reproduces); `still-standing`'s `dim`
toggle, its missing `.dim` / `.quiet` / `.holds` / `.neighbor` rules and its missing flag
writers; and the `id="send"` kill that `test/still-standing.test.js` alone lacks.

---

## Ground truth at the sweep

- Boards `C:\dev\skyrail-boards` at **`c8c4546`**, clean (`git status --porcelain` empty
  before and after). `node --test test/*.test.js` re-run here: **tests 814, pass 814, fail 0**,
  25.7 s. PWA `C:\dev\skyrail` at `840dbd0`, never opened. No git write, dev server, browser,
  deploy or QA harness touched either repository; scratch is `C:\tmp\domsweep\`.
- **The tree moved under the sweeps, and the corpus is not the brief's.** The brief said
  twelve boards at `919b6be`, ~6,450 lines, suite 717. The four sweepers measured at
  `919b6be`, `ab18d70` and `e8904de` (docs-only drift, every line number valid); the four
  refuters at `ab18d70`, `6e95383` and **`c8c4546`**. Between them `sit/still-standing/`
  landed (`4b08539`, 2026-09-04, C14 / CFD-212) and `test/lexicon-ledger.js` declared it
  **CITY**. `git diff --stat 919b6be..HEAD -- sit/ public/ test/` is 13 files: the new board
  (index.html 478, sim.js 833, MANIFEST 134, byte-identical `public/` twins), four new test
  files, +97 lines to the ledger, and **+19 lines to `test/two-ways-from-here.test.js`** — which
  is why every `two-ways-from-here.test.js:N` cite in the text sweep is 13 lines stale. The
  wiring and text refuters swept still-standing; the ids and classes refuters predate it.
  **Where this record says "thirteen", it was measured here; where it says "twelve", it is
  the pair's figure at its base and still true of those twelve.**
- **The fourteen pages the brief named are fifteen at HEAD**, 6,928 lines by `wc -l`,
  LF-normalised — the brief's 6,450 was right at its base:

  | page | lines | lineage |
  | --- | --- | --- |
  | `sit/dawnspur-heat/index.html` | 291 | DESK |
  | `sit/dawnspur-scale/index.html` | 310 | DESK |
  | `sit/dawnspur-dispatch/index.html` | 494 | DESK |
  | `sit/dawnspur-line/index.html` | 610 | DESK |
  | `sit/dawnspur-storm/index.html` | 733 | DESK |
  | `sit/dawnspur-site/index.html` | 336 | CITY |
  | `sit/dawnspur-halt/index.html` | 393 | CITY |
  | `sit/mosswake-loop/index.html` | 409 | CITY |
  | `sit/herbs-larder/index.html` | 412 | CITY |
  | `sit/they-remember/index.html` | 422 | CITY |
  | `sit/dice-at-the-places/index.html` | 450 | CITY |
  | `sit/two-ways-from-here/index.html` | 470 | CITY |
  | `sit/still-standing/index.html` | 478 | CITY — **landed after the brief** |
  | `public/dawnspur/index.html` | 294 | PUBLIC_ONLY (the preserved kill, CFD-176) |
  | `public/convoy-stop/index.html` | 826 | PUBLIC_ONLY (sim exports nothing; UNDERIVABLE) |

  `public/<board>/index.html` is byte-identical to its `sit/` twin for all **thirteen**
  (`cmp -s`, 13/13, this synthesis; the pairs checked twelve by `git hash-object` and `cmp`),
  so sweeping `sit/` is sweeping what ships. The hub `public/index.html` is out of scope and was
  measured to be so: zero `id=`, zero `data-*`, zero `<script>`.
- **The lineages are declared, not inferred** (`test/lexicon-ledger.js:37-41`, the
  orchestrator's ruling of 2026-09-01): DESK = heat, scale, dispatch, line, storm; CITY = site,
  halt, mosswake-loop, herbs-larder, they-remember, dice-at-the-places, two-ways-from-here,
  still-standing. Every seam count below is under that membership.
- **What the landed guard already covers, and what it can see.** `DOM_ROWS` holds three rows —
  `lit` (`lit`/`unlit`, MEDIUM), `home` (`home`/`at-halt`, HIGH), `train/consist` (LOW). Its
  instrument, `deriveDom()` (`test/lexicon-derive.js`), reads exactly three sets: `id="..."`,
  the first argument of `classList.toggle("...")`, and string literals on the right of
  `.className = ...`. It states its own limit in its header: a class set any other way
  (`classList.add`, a template string, a static `class=""` attribute) is not seen. The test that
  grades it (`test/lexicon.test.js`, the *DOM: lit / home / train-consist* test) iterates the
  literal list `["ids", "toggles", "classNames"]` — **so a row keyed on any other set is not
  graded as drift; it is silently skipped and passes green.** Measured cost on the class
  surface alone: of 655 token/board/kind cells the instrument sees 126 and is blind to 529; 71
  tokens exist only in cells it cannot see. Every disposition below is shaped by that number.
- **Two figures in the brief's own source do not reproduce.** Audit §3.2 names `data-action`
  attributes; `grep -rn 'data-action' sit/ public/` returns zero hits — the whole `data-*`
  inventory is `data-route` (dispatch/line/storm, four values) and `data-i` (convoy-stop, five).
  And "4,860 lines" was 5,330 at the brief's base (exactly 4,860 plus two-ways' 470) and is
  5,808 for `sit/` at HEAD. Both are the figure-goes-stale-by-one-board shape the audit's own
  header warns about.

## Method

Four sweepers and four refuters, eight agents, read-only. Each sweeper built a mechanical
enumerator for its surface, verified the sit/public precondition before relying on it, drove
every claim that was behavioural, and reported clean results as results. Each refuter built its
**own** instrument rather than re-running the sweeper's — region-aware id extraction with an
idref channel; a class parser that blanks style/script/markup regions and splits balanced
`classList` arguments; a page **executor** that runs each inline `<script>` under `node:vm`
against a recording stub DOM so a binding is what `addEventListener` actually received; a fake
DOM that renders each page and reads text nodes and aria back after every gesture — and then
reproduced the sweeper's headline figures before grading a single row. All four did reproduce
them, to the token, which is the sweep's first clean result: **99 ids / 8 seam, 119 classes /
3+6 seam, 120 wiring tokens / 11 seam, 267 words / 69 seam** were each counted twice by two
instruments and agreed.

The synthesis then kept CONFIRMED and PARTIAL rows whose refuter agreed the *action*, took the
refuter's severity where its correction was measured rather than argued, dropped rows where the
two readers disagree on the action into *do not average*, merged the same token found on two
surfaces (terrace: ids + wiring; step: classes + ids-refuter; hearth: ids + classes) into one
finding, and re-derived every proposed row's board list at HEAD before writing it — because the
board list is the only thing the guard can grade, and eleven of the pairs' rows named "five map
boards" that are six.

**Three things this sweep did that the 2026-09-02 canon sweep did not, all forced by the
subject matter.**

1. **The reference channel was closed before the counts were trusted.** Every `getElementById`
   argument in 6,450 lines is a string literal (grep for a non-literal argument: zero), so
   "0 unreferenced ids" and "2 dangling references" are assertions, not samples. Two refuters
   added channels the sweepers lacked (idref attributes; `classList.add/remove/replace`) and found
   nothing extra — recorded as a result, because it says the landed regex's known weaknesses do
   not bite in this corpus.
2. **Reachability was enforced on the classes, not just the sims.** The classes sweeper flagged
   halt's ruleless `gone` as a live defect and then refused its own finding: `commitLand()` is the
   sole writer of `inbound` and `landed` on halt and site and sets them together, so the branch
   cannot fire. Its refuter enumerated both boards' full state spaces (halt 5, site 4, strictly
   linear) and agreed. The sweeper's first enumerator also harvested `"rs"` and `"lv"` out of
   concatenations and reported 24 dead rules that are live; the refuter made the identical mistake
   independently and caught it the same way. Both are recorded so nobody re-finds them.
3. **A drive that measures refusal was named as such.** CLS-2's evidence sentence drove four heat
   verbs "from the opening"; its refuter reproduced it literally and three of the four are refused
   and change nothing — a reading taken from a state reached only by refusal, the fixture rule
   from `CLAUDE.md` paying out on a class token. The finding survives by a different route
   (`wait()` at `sit/dawnspur-heat/sim.js:74`), and the record carries the corrected route.

## Rows by surface

| surface | tokens | seam | sweeper findings | C / P / R | severity agreed | refuter missed | uncheckable |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ids | 99 | 8 | 5 | 3 / 2 / 0 | 2 of 5 | 3 | 2 |
| classes | 119 | 9 | 7 | 6 / 1 / 0 | 6 of 7 | 5 | 5 |
| wiring | 120 | 11 | 7 | 6 / 1 / 0 | 4 of 7 | 3 | 2 |
| text | 267 | 69 | 7 | 4 / 3 / 0 | 1 of 7 | 4 | 1 |
| **total** | — | — | **26** | **19 / 7 / 0** | **13 of 26** | **15** | **10** |

Every figure in that table is read from `results.json`, not typed. **No sweeper row was
REFUTED outright** — every `file:line` in 26 findings reproduced under a second instrument —
and the refuters' work went instead into severity (13 of 26 disagreed, twelve of them
downward) and into the axis the sweepers' instruments structurally could not see: a token that
is an *id* on one lineage and a *class* on the other never appears on an id-to-id seam table,
and a token that splits between `dawnspur-halt` and `dawnspur-site` — two CITY boards that are
near line-for-line mirrors — never appears on a desk-vs-city table at all. Nine of the fifteen
missed rows are one of those two shapes.

The seam figures are two senses, and the record keeps both: 8 / 3 / 11 are tokens present on
both a DESK and a CITY page in the *same* channel; the classes surface adds 6 (`glass`, `haul`,
`hearth`, `home`, `stack`, `step`) that are a class on one side and an element id on the other —
the ledger's own `home` shape. The text surface's 69 is at twelve boards; **at HEAD it is 75**,
the six new words all still-standing's, none a meaning split.

## Counts

**Failure mode**, over 26 sweeper findings as filed: SILENT 22 · LOUD 1 · NONE 3. Over the 15
refuter-missed: SILENT 11 · NONE 4 *(the synthesis's classification of mixed verdict strings; a
re-count at review gives 9 bare SILENT, 4 beginning SILENT, 2 beginning NONE)*. **Silent is worse, and this corpus is almost entirely silent:**
the two lineages share so little control vocabulary that the copy that fails loudly is the rare
case (`terrace`'s tap handler, `#carry`/`#up`/`#tend` onto a city board), and what fails silently
is a rule that matches nothing, a class that paints nothing, or a handler that returns `false`.

**Severity, as resolved by this synthesis** (sweeper grade → refuter grade → carried):
HIGH **1** (TXT-1, the rendered Halt / home referent swap — agreed HIGH by both readers, and the
only finding in the sweep where both said HIGH) · MEDIUM **14** · LOW **10** · NONE **8** ·
instrument-only **5** (findings whose whole content is that the guard cannot see them) *(these sum
to 38 against 26 findings + 15 missed = 41, and the ranked list below carries 27 items with MEDIUM
at 16 by the review's count — an unstated basis, recorded here rather than reconciled)*.

**Action:** DOM_ROWS proposed **11** (7 agreed + 4 refuter-only), all graded green at HEAD ·
amendments to existing rows, prose only, **3** · instrument extensions owed **6** · board-local
test edits for the orchestrator **2** · DAVID **6** · NONE / clean **17 tokens on the seam**.

## The findings, ranked

Ranked by failure mode first (silent before loud before none), then severity, then whether both
readers agreed. Each carries the sweeper's evidence and the refuter's correction in its own words.

**1. HIGH, silent — the rendered name of the Halt, and `home` with it. Agreed by both readers;
the only finding both graded HIGH. (text TXT-1; TXT-2 folded in.)**

> Sweeper: *"DESK dispatch/line/storm: an AWAY DESTINATION. `cards()[0].name` is the string
> "DAWNSPUR HALT" … spoken as the aria-label "DAWNSPUR HALT, pays 10, stake 0, 68 percent home"
> (dispatch/index.html:345, line:419, storm:498) … CITY all six: `aria-label="the Halt"` on the
> tappable place … and every sentence — "The Halt holds.", "The Halt is awake." — is HOME, the
> place every send leaves FROM."*
>
> Refuter: *"CITY aria-label="the Halt" on SEVEN boards at HEAD … still-standing:367 … The desk
> carries BOTH senses on one board, in one rendered line: storm appends SKY_TO_BIRD (sim.js:129)
> to runSentence, so the page shows "The train left for Dawnspur Halt with nothing staked. A
> stormbird is inland over the halt." — the card's Halt is the destination, the weather's halt
> is where the desk stands. Strengthens HIGH."*

`ROWS.halt` (audit §1.4, HIGH) grades the *id* surface — route id vs place id. The rendered
name is a different surface and no row covers it: `<title>Dawnspur Halt</title>` on heat, scale
and halt is a page identity; `DAWNSPUR HALT` on three desk card faces is an away destination;
`the Halt` on seven city aria-labels is home. TXT-2's `home` was refuted as a *separate* finding
— the word means the return point on both sides and the odds phrasing agrees (`68 percent home`
/ `Home she comes. 64.`); what swaps is the *name* of that place, so it is recorded here once.
Storm's card aria `"DAWNSPUR HALT, pays 10, free, …"` is the rendered origin of the docs-side
"free Halt" shape `test/beats-lexicon-ledger.js` already grades. **Not a row**: needs a `labels`
/ `sentences` deriver *and* a change to the test loop's hard-coded set list — the refuter's
correction that an unknown set key is "silently skipped, not drifted" is why. See *Dispositions*.

*(2026-09-09, by counsel on David's instruction — entry 46 of
`docs/decisions-open-2026-09-02.md`. RULED: (b) — the rendered name stays; no board byte moves;
the split is carried as a declared row once a `labels`/`sentences` deriver exists (owed,
ranked sixth under *Instrument extensions owed*, below). Option (a) — re-voicing the three
desk card faces — is refused as an act and put to David as a question, not withheld. Five
corrections ride with the ruling — this read "Four" until the fix pass of 2026-09-09, written
before amendment (iv) was carried as (v) below.*

*(i) `THE ROW MUST NOT SETTLE THE WORLD.` David's own signed and passed beat calls this two
meanings, not one place: `docs/cfd-210-beat.md:379` — "'Send her to the Halt' means sending
her where she already is" — which closes the passage at `:376-379` *(corrected at the fix pass,
2026-09-09; this cited `:377-378`, the `consistAt === "halt"` / `canDo: null` sentence before
it, and stamped the cite "verified" — re-measured here, `grep -n` returns the quoted sentence
at `:379` only)* — and `:382-384` — "Two boards, one word, two meanings: home in one
model, a place you send to in the other" (the quoted sentence itself runs `:383-384`; verified,
this worktree). The World Bible files the
place as home base
(`C:/dev/skyrail/docs/lore/weekend-worldbuilding-2026-06-29/source/Skyrail-Reclamation-World-Bible-v0.1.md:353`;
`C:/dev/skyrail/docs/lore/weekend-worldbuilding-2026-06-29/source/Skyrail-Reclamation-Master-Design-Bible.md:92`,
both re-read at the fix pass — the ruling's amendment (E) asked that the Master Design Bible
cite carry its full path, and the World Bible's is given in full beside it for the same reason);
neither settles whether the desk's route is that same place. The pack's own gloss, "the home
halt" (`C:/dev/skyrail/src/content.js:381`, verified), is the audit's filed BRIDGE
(`docs/name-collisions-audit-2026-09-01.md:238`), not evidence to adopt as this row's wording.*

*(ii) `THREE PASSED DESK BOARDS` IS WRONG. `dawnspur-dispatch` passed 2026-08-26 and
`dawnspur-line` passed 2026-08-28 (`public/index.html:91`, `:85`); `dawnspur-storm` is live,
"sat five times, stopped without a pass" (`:79`). The lineage lock covers passed and killed
alike, so this correction does not make option (a) available.*

*(iii) `"STORM RENDERS BOTH SENSES IN ONE LINE"` IS HALF MEASURED, HALF UNPROVEN. Measured, this
worktree, from the minted opening (marks 3, stores 0, sky clear — the roll is stubbed only for
the run outcome): three send-halt/meet cycles yield the single `#say` string, via `spoken()`
(`sim.js:657-661`, joins `s.sentence` and `s.skySentence` with a space) into one text node
(`index.html:581-582`): "The train left for Dawnspur Halt with nothing staked. A stormbird is
inland over the halt. There is a storm coming in behind it." The co-occurrence in one rendered
line is real. UNPROVEN: that the second occurrence carries the HOME sense. The same board's
`sim.js:130` names the desk's own ground "The storm is over Dawnspur..." — never "the halt" —
and the MEET line renders "...short of the halt. The train turned for home with the haul
unbanked... the train is home, and the desk stands" in one string: lowercase "the halt" as
destination, "home" as where the desk stands, in the same sentence. That points against reading
the weather's lowercase "halt" as the desk's own ground.*

*(iv) NO BOARD CARRIES BOTH SENSES AS PAGE IDENTITY AND DESTINATION. The three boards carrying
the card-face string "DAWNSPUR HALT" (dispatch, line, storm) are titled Switchyard / Line /
Storm; the three titled `<title>Dawnspur Halt</title>` (heat, scale, halt) carry that card-face
string zero times and no `ROUTES` at all.*

*(v) — THE RULING'S AMENDMENT (iv), ADDED AT THE FIX PASS, 2026-09-09, HAVING BEEN DROPPED FROM
THE FIRST CUT OF THIS BLOCK. This finding's own closing sentence above rests on the refuter's
correction that an unknown set key is "silently skipped, not drifted". That is **amended, not
superseded**: verified at `e226a9f`, the drift loop still skips silently — its set list is
hard-coded `["ids", "toggles", "classNames"]` at `test/lexicon.test.js:571`, so an undeclared
key never enters the comparison — but the can-go-red test at `:606-620` now refuses the
declaration **by name**, printing that the set "is not one the derivation produces … the drift
loop skips it in silence, so this half of the row grades nothing". A `labels` row declared today
therefore lands **RED**, not silently green. The instrument extension is still owed; what is no
longer true is that such a row would pass unnoticed.*

*No board byte moves. The instrument extension (a `labels`/`sentences` deriver) is owed
test-only work the orchestrator may schedule on its own authority; the council does not order
it as a fourth mechanism. See David's calls, below, and entry 46 for the full question.)*

**2. MEDIUM, silent — `dim`: a styled affordance on the desk, an unstyled constant on the city.
Agreed. (classes CLS-1.) Row.**

> Sweeper: *"`ladderEl.classList.toggle("dim", board.musterReach === 0)`: the MUSTER ladder is
> OUT OF REACH. An affordance, and it is STYLED … CITY … `mossEl.classList.toggle("dim",
> board.mossDim)` on `#mosswake`: the Mosswake glasshouse is SCENERY … there is NO `.dim` RULE
> on any of the five pages … mossDim=true on ALL FIVE city boards at frame one."*
>
> Refuter: *"The severity rests on the DESK side … `mossEl.classList.toggle("dim",
> board.mossDim)` carried desk-ward reads board.mossDim as undefined, so the MUSTER ladder
> silently stops dimming when reach falls to 0 and a live affordance is lost. … `grep -n
> 's\.mossDim\s*='` over all five city sims returns nothing, so mossDim has no writer at all
> and is a compile-time constant true on every board."*

Verified here at HEAD: still-standing toggles it too (`:427`), has no `.dim` rule and no writer;
the row carries nine boards.

**3. MEDIUM, silent — `step`: an element on heat, a state class on halt and site, and the types
invert the failure. Agreed; found twice (classes CLS-2, ids-refuter). Row.**

> Sweeper: *"`board.step` is a STRING, opening "off" … `board.heatStep` is a NUMBER, opening 0
> … `groundEl.classList.toggle("step", board.step)` carried desk-ward is permanently ON ("off"
> is truthy); `stepEl.className = board.heatStep === "out" ? ...` carried city-ward is
> permanently "" (a number never equals "out"). Neither throws."*
>
> Refuter: *"The finding stands; its evidence sentence does not. … commitB, commitHold and
> commitWarm all return FALSE and change nothing; only commitGoods is accepted. So three quarters
> of that drive measured a state reached by refusal … the mechanism is wait() at sim.js:74,
> `if (s.step === "out") s.step = "gone";`, which its own drive never exercised."*

The ids refuter reached the same token from the other channel and added the third silent failure:
heat's idiom is a wholesale `.className =` assignment, so ported to a city board it would erase
`#ground`'s `step` class along with every other class on that element.

**4. MEDIUM, silent — `terrace`: three element kinds under one id, and a control on one of them.
Sweeper HIGH, refuter MEDIUM; MEDIUM carried. (ids IDS-1 + wiring W5.) Row.**

> Sweeper: *"a FIVE-STEP LEVEL GAUGE of board.reserve … (line:254, storm:290) … CITY
> dawnspur-site: a BOOLEAN food-present indicator … CITY dawnspur-halt: an INTERACTIVE PLACE.
> `<button type="button" id="terrace" aria-label="greenhouse terrace">` (:301) … AND a handler
> at :382 … Three element kinds, three semantics, one id."*
>
> Refuter: *"The prose half of the HIGH argument is REFUTED. "The only governing sentences about
> a terrace in the design record are the DESK ones" is false. `grep -rc terrace docs/*.md` puts
> eight in docs/cfd-205-halt-beat.md alone … Every one is the city boolean sense and every one
> is correct. … The ledger's own MEDIUM, `lit`, is the strictly harder case: same class token,
> same classList.toggle idiom, no structural tell at all. terrace cannot grade above lit."*

The refuter also supplied the null the sweeper had in the ledger and did not use: `dawnspur-site`
is a *demonstrated* desk-to-city copy vector (it carries `train`, `hearth`, `platform`), and when
that copy ran, site's terrace was re-authored correctly. The wiring pair graded the same token
LOW / LOUD from the binding side — every one-step copy of halt's handler throws — and both
readers agreed; the row carries the control/scenery split as one sentence per board.

**5. MEDIUM, silent — `hearth`: permanent scenery on the desk, a hidden state indicator inside
site's `#foundry` button. Agreed. (ids IDS-4, with two corrections.) Row.**

> Sweeper: *"`#hearth { display: none; ... }` at :147, revealed only by `#foundry.live #hearth`
> … On site the hearth being visible IS the proposition "the foundry has been rebuilt" …
> site.createBoard({fresh:true}).foundry === false … HIDDEN at frame one — the exact opposite of
> the desk hearth."*
>
> Refuter: *""The one desk board that DOES export hearth: "held" is dawnspur-scale" is wrong.
> … `"hearth" in board` is TRUE on heat (value "off") and on scale (value "held") … So the
> disjointness is 2-vs-3, not 1-vs-3 … OMISSION: `hearth` is a CLASS on six city boards …
> site is the ODD BOARD OUT: it is the only page in the corpus where the foundry's interior
> parts are ids rather than classes."*

Seven at HEAD: still-standing:375 `<span class="hearth">`. The class half is ungraded by the
instrument and is a comment inside the row.

**6. MEDIUM, silent — `foundry`: same id, same tag, same event, a COMMIT on site and a SELECT on
halt. Agreed; intra-CITY. (wiring W4.) Row.**

> Sweeper: *"site->halt (the silent direction): dawnspur-halt DOES export commitSite, so the
> copied handler runs — `halt.commitSite('foundry')` from the opening returns false and the full
> non-function state snapshot before and after is byte-identical … a button that looks alive,
> is tapped, does nothing, and reports nothing. … halt->site … throws `TypeError:
> s.postNotice is not a function`."*
>
> Refuter: *"The not-a-double-bind claim is confirmed by execution rather than reading: site's
> setup produced exactly 2 bindings (#foundry pointerdown and document touchstart), so asButton
> at :270 returned early on the first paint(); after the first tap the :328 handler sits on the
> detached old button and the new DIV carries none."*

**7. MEDIUM, silent — `platform`: px and load-bearing on the desk, percent and inert on site and
halt, absent on the six map boards. Agreed. (ids IDS-3.) Row.**

> Sweeper: *"A `#platform` rule copied onto any of the five two-ended city boards matches no
> element at all — the ground never paints, and because those boards already paint ground
> through platform-halt/platform-moss the page still looks plausible."*
>
> Refuter: *"site and halt ALSO carry a distinct `#ground` element that IDS-3 does not mention
> … So the desk's SINGLE `#platform` maps to a city PAIR … while the five two-ended boards split
> the same job a third way … Four spellings of one fiction across the corpus."*

**8. MEDIUM, silent — the verb-dispatch shape: one control per verb on the desk, two generic
controls on the city, and every real city verb named by no control. Sweeper HIGH, refuter
MEDIUM; PARTIAL. (wiring W1, with the refuter's missed M1.) Not a row — instrument.**

> Sweeper: *"via the shipped path (postNotice('lamp') then commitPosted()) the state carries
> `lampLit:true` AND `posted:"lamp"` … via a directly-wired commitLight() the state carries
> `lampLit:true` and `posted:null`, postedNotice() = null. Identical world, one missing notice,
> no error."*
>
> Refuter: *"Every desk verb id but two names a method no city board exports … so
> #carry/#up/#tend/#meet/#ranger/#track/#go copies throw `is not a function`: LOUD. … `id="send"`
> is suite-forbidden on 7 of 8 city boards' own markup … Net: exactly ONE silent cell — a #send
> control on still-standing, the one city board whose test carries no id="send" kill."*

Verified here: `grep -n 'id="send"' test/still-standing.test.js` returns nothing *(superseded at
landing, 2026-09-08 — the grep is true and the conclusion drawn from it was false. A verbatim desk
`<button … id="send">` on still-standing fails `test/still-standing.test.js:1470-1472`, the
whole-button-id enumeration — `["consist","halt","mosswake","notice-do","rustfall","send"]` — and
`:1627`'s desk-shape kill, so the copy is LOUD, not silent, and "exactly ONE silent cell" above is
the refuter's claim carried past what the grep could see. What was open was narrower: a non-button
element carrying `id="send"`, which the enumeration does not count, and uniformity with the five
sibling tests that carry the exact four-token regex — five, not seven; halt:532 and site:325 carry
a different `id="send"|data-route|…` kill. Closed at `925d23a`. This read "the seven other city
tests carry the kill. That one-line gap is a board-local test edit, not a ledger row.")*.

**9. MEDIUM, silent — the route-name carrier split: `data-route` values in long form on the desk,
element ids in short form on the city. CONFIRMED, HIGH → MEDIUM for the new half. (wiring W2.)
Not a row — instrument.**

> Sweeper: *"canSend('halt') / ('mosswake') / ('rustfall') / ('consist') = false on
> dawnspur-dispatch, dawnspur-line and dawnspur-storm — 12 of 12 false … a card carrying the
> city's spelling renders permanently unlit and disabled and never sends, with no error."*
>
> Refuter: *"the HIGH half (a desk-spelled commitSend on a city board sends to Mosswake and
> returns true) is ROWS canSend/commitSend #2 … already adjudicated on the export surface. The
> NEW half is the carrier split … whose wrong action is a permanently unlit, disabled card on the
> desk — silent to a developer, visible to a player. That half is MEDIUM. … rustfall is a place
> on three city boards (dice, two-ways, still-standing), not two."*

**10. MEDIUM, silent, intra-DESK, shipped — `/dawnspur-line/` speaks "stake undefined" on every
sendable card that is not *out*, at every frame — three of four at the opening, two of four with a
send in the air; the *out* branch of `index.html:413-420` says "No Wardens ride with nothing staked"
instead *(superseded at landing; this read "on every sendable card at every frame")*. Refuter-only
(text). Verified here.**

> Refuter: *"dispatch/index.html:345 builds the card aria as `c.name + ", pays " + c.pays + ",
> stake " + c.stake + …` where cards() carries `stake`; line/index.html:419 is the same template
> byte-for-byte, but line's cards() keys are `id name line sendable lit out pays provisions toll
> chance percent note condition provisional manifestLine` — no `stake` … Storm avoided it with
> its own stakeText … Sighted players are unaffected (.cs uses stakeText); screen-reader players
> hear the defect."*

Reproduced by this synthesis: `sit/dawnspur-line/index.html:419` reads `c.stake`;
`createBoard({fresh:true}).cards()[0]` has no `stake` key on line and `stake: 0` on dispatch.
This is the audit's #1 (`provisions` — marks on dispatch, food on line) surfacing as rendered
text, which the text sweeper's clean result said never reaches the player. No test reads line's
aria-label; `stake undefined` appears nowhere in `docs/` or `test/`. **The fix is a board edit to
a passed, hash-pinned board** — a David call, below.

*(2026-09-09, by counsel on David's instruction — entry 42 of `docs/decisions-open-2026-09-02.md`.
RULED: (b) — leave the passed bytes of `/dawnspur-line/` untouched and record the defect; the
rule question (may a pass's freeze exclude spoken accessibility text?) goes to David.*

*THE EXACT PATCH, corrected from what this finding and the entry both first assumed. Storm's
bare `stakeText(c)` is NOT the fix — it would restore the stake and still speak the wrong thing
on a sendable-but-unlit card, since `stakeText` never returns the condition. The parity form
mirrors line's own visible face at `index.html:404`: `c.name + ", pays " + c.pays + ", " +
(c.condition === null ? stakeText(c) : c.condition) + ", " + c.percent + " percent home"`.
Verified against source at `e226a9f`: `index.html:404` reads
`c.condition === null ? stakeText(c) : c.condition`; `index.html:419` interpolates
`", stake " + c.stake`; `sim.js`'s `cards()` (`:347-373` — corrected at the fix pass, 2026-09-09;
`:346` is the previous function's closing brace, `cards` opens at `:347` and its own closing
brace is `:373`, not `:372`, which closes the `.map()`) returns keys `id name line sendable
lit out pays provisions toll chance percent note condition provisional manifestLine` — no
`stake`, in both the sendable and non-sendable branches.*

*THE PIN CENSUS, re-measured at this worktree's `e226a9f`: `grep -rn "b6f21db0" . --include=*.md
--include=*.js --include=*.txt --include=*.html` (excluding `node_modules`, and excluding this
file, which carries no occurrence at `e226a9f`) returns **54** lines — matching the count
carried by the ruling: 53 lines of pin census (the hub card at `public/index.html:87`; 16
full-sha lines and 6 short-sha entries across 8 descendant test files, spot-checked —
`test/dawnspur-halt.test.js:85-86` carries the sit/public pair; `sit/dawnspur-line/MANIFEST.txt:102`
and its `public/` twin, verified, both read
`sha256:b6f21db0dd8a2d4ee9373f859859748c5c0778204a3cad6ee95466c338d43f2a`; 18 lineage lines
across 9 other boards' sit+public MANIFESTs; and 10 lines across 9 signed beat documents) plus
one non-beat hit outside the census, in `docs/decisions-open-2026-09-02.md` entry 27 (its quotation of the `/dawnspur-line/` PASSED row). Seven
of the eight descendant tests belong to boards that have **passed** their sit — the eight are
`test/dawnspur-halt`, `dawnspur-site`, `dice-at-the-places`, `herbs-larder`, `mosswake-loop`,
`still-standing`, `they-remember` and `two-ways-from-here`, and their own hub tags, verified at
`e226a9f`, are `public/index.html:31` (still-standing, "passed its sit 2026-09-04"), `:37`
(two-ways, 2026-09-02), `:43` (dice, 2026-09-01), `:49` (they-remember, 2026-08-31), `:55`
(herbs-larder), `:61` (mosswake-loop) and `:67` (dawnspur-halt), all "passed its sit";
`dawnspur-site` is the one exception, `:73` "sat 2026-08-30, not passed" *(corrected at the fix
pass, 2026-09-09; this cited `:85` and `:91` — the line board itself and dispatch — which are
true "passed its sit" lines but are not among the eight descendants, so a reader who checked
them could not confirm the sentence)* — and the board is one of **15** hub cards, not sixteen.*

*A NEWLY MEASURED FINDING, not fixed here: `/dawnspur-storm/` (live, `public/index.html:79`
"sat five times, stopped without a pass") carries the lesser half of the same divergence.
Verified: its visible face at `sit/dawnspur-storm/index.html:469` is
`c.condition === null ? stakeText(c) : c.condition`; its aria at `:497` is bare `stakeText(c)`.
Belongs to the beat that owns storm's lineage, not to this council or this record.*

*No board byte moves under this ruling. A self-cancelling guard — green at HEAD, red on the fix,
printing this parity patch and pin census — is ORDERED by the ruling and is **NOT built**: it is
owed as an append at the end of `test/dawnspur-line.test.js`, outside this batch's glob, and the
ruling requires it be mutation-verified red-first before it lands — "if the new guard is NOT
among the reds, it is decoration and must not land" *(corrected at the fix pass, 2026-09-09;
this read "is built in `test/dawnspur-line.test.js`" — measured, `git status --porcelain test/`
is empty at `e226a9f` and `grep -n "stakeText" test/dawnspur-line.test.js` returns nothing, so
no guard exists and the sentence promised a reader a protection that is not there)*. See
David's call, below.)*

**11. MEDIUM, silent — `live` on site: world state on `#foundry`, a selection ring on `#frame`
and `#train`, eleven lines apart in one `paint()`. Refuter-only (classes). Row, refuter-only set.**

> Refuter: *"Driven over dawnspur-site's complete reachable space … The two senses DISAGREE in
> 2 of the 4 states and never agree in the affirmative: at the opening the foundry is selected
> but not working, and after commitCast it is working but not selected. … The sweeper reported
> `on` as its strongest CLEAN result … That is true of the token `on` and conceals this: on the
> single CITY board that carries `on`, the identical visual vocabulary is ALSO reached through
> `live`."*

The wiring refuter flagged the same token independently from its side. Read by no second
reader on its own surface, so it is in the refuter-only set.

**12. MEDIUM, silent — `haul`: a haul waiting at the destination (desk id), a haul on the vehicle
(dice, two-ways class), and HERBS on the vehicle under the same span and the same `hauled` class
(mosswake, herbs-larder, they-remember). Refuter-only (ids). Row, refuter-only set.**

> Refuter: *"a beat sentence — "the haul shows" — lands on the destination on one lineage, on the
> vehicle on the next two boards, and on the HERBS on the three after that … test/lexicon-ledger.js
> lists `haul` in UNADJUDICATED.desk while `haulOnConsist` and `herbsOnConsist` are in
> UNADJUDICATED.city, so the name-and-arity instrument correctly sees three separate names that
> never cross the seam. Only the DOM token is shared."*

**13. MEDIUM, silent — `stack`: the desk's whole control column vs the foundry chimney.
Refuter-only (ids). Row, refuter-only set.** *"judged on the copied-rule consequence alone this
is LOW (a rule that matches nothing), and it is the sentence-level ambiguity that carries it to
MEDIUM"* — the refuter's own split, recorded as given. Absent from every ledger list because it
is not a sim name anywhere.

**14. MEDIUM, silent — `#go` with `#goodsA` / `#destA`: two disjoint verb dispatchers under one
id inside DESK, and the copied chain animates the shuttle with the world unmoved. Refuter-only
(wiring). Row, refuter-only set.** *"4 of 4 copy cells: no throw, no commit, animation plays"* —
the decorative motion `CLAUDE.md` forbids, reachable by pasting a four-line chain that exists
verbatim in both files.

**15. MEDIUM, silent — the Halt lamp's parts under three simultaneous renames (`#lamp`→`#halt`,
`.post`/`.arm`→`.lamp-post`/`.lamp-arm`, `lit`→`remembered`), `.globe` alone keeping its name.
Refuter-only (classes). Amendment to `DOM_ROWS.lit`, prose only** — static classes the
instrument cannot grade.

**16. MEDIUM, silent — `posted` / `filled`: the same class, the same condition, hung on
`#foundry` on halt and `#frame` on site. Refuter-only (classes); intra-CITY; the JS direction is
loud (`frameEl` undefined on halt). Note only** — a row would grade membership (both toggles on
halt and site) and not the host, which is the mechanism.

**17. MEDIUM, silent — `hud` / `marks-line`: the readout itself on heat and public/dawnspur, a
container on eleven (twelve at HEAD). Sweeper HIGH, refuter LOW; PARTIAL. (ids IDS-2.) Not a
seam finding; note only. See *do not average*.**

> Sweeper: *"assigning to `.textContent` on an element removes every child node. So heat:200
> copied onto any of the eleven deletes #marks-line from the document. It does not throw …
> On scale it is worse: … the UP control leaves the document with its handler still bound."*
>
> Refuter: *"`#hud` is a CONTAINER on all seven CITY pages AND on four of five DESK pages …
> The meaning does not vary by lineage — it varies inside DESK. That is exactly what the sweeper
> wrote about marks-line in its own clean result #3 … NO page among the eleven binds a `hud`
> identifier … the described one-line copy is really a two-line copy."*

**18. LOW, silent — the id-vs-class shape for scenery parts (`hearth`, `stack`, `glass`, `haul`),
and its intra-CITY twin (`scaffold`, `bill`, `hearth` between halt and site; `tend` between
scale and storm). Sweeper LOW, refuter MEDIUM-and-rescoped; PARTIAL. (classes CLS-5 + ids IDS-5
+ classes-refuter M4.) One comment above `DOM_ROWS`, not four rows.** The refuter's correction
is the load-bearing one: `sit/dawnspur-site/index.html:235 <span id="hearth">` — site is a
declared CITY board that takes the desk shape, so the split is not the seam and the danger is
halt-vs-site, where the rules differ by a single `.` versus `#` under an identical state-class
half. `glass` adds a residue: line and storm carry a dead `#glass` rule byte-identical to
dispatch's while declaring no element, and the obvious "fix" reactivates the flat box on top of
the graded `#greenhouse`.

**19. LOW, silent — `quiet` / `holds` / `neighbor` (and `dim`): world-state classes toggled on
`#mosswake` and `#halt` with no rule on any page. Agreed; intra-CITY. (classes CLS-6.) DAVID.**

> Sweeper: *"either these are DEAD WIRING (the rules were never written, and the fix is to
> delete four toggles across four boards) or they are UNWRITTEN ART (the rules are owed, and the
> fix is four CSS rules). I cannot tell from the source, and I am not going to guess."*
>
> Refuter: *"`grep -n "s\.\(mossDim\|mossQuiet\|haltHolds\|neighborAgain\)\s*="` across all five
> city sims returns exactly ONE line in the entire corpus: sit/herbs-larder/sim.js:132
> `s.neighborAgain = true;` … on four of five boards these toggles cannot animate even if the
> rules were written, which is evidence for DEAD WIRING; herbs-larder's neighborAgain is the
> single flag that actually transitions."*

Verified here: still-standing toggles all three, has no rule for any, and has no writer.

**20. LOW, silent — `UP` / `up`: the greenhouse-raise verb on the desk, the stow-in-larder verb
on the city, and a third sense (erected) inside CITY. Sweeper MEDIUM, refuter LOW; PARTIAL.
(text TXT-3.) Note only** — nothing keys on the word; a copied sentence misreads, nothing
executes wrongly. Exclude *"People remember who showed up."* from any future row.

**21. LOW, silent — the desk voice in dead city prose: `"The train brought the Mosswake cargo
home."` assembled on dice, two-ways and still-standing, boards whose vehicle is the consist and
whose home is the Halt; rendered by none. Sweeper MEDIUM, refuter LOW. (text TXT-5.) DAVID.**
Three carriers at HEAD (still-standing:601); unrendered on every page, unquoted in every doc;
the sweeper's dice pin (`:481`) is the failure sentence, not this one, and its two-ways cites are
13 lines stale. The cheap guard `assert.doesNotMatch(SIM_CODE, /The train brought/)` is red on
three boards today and can only go green by editing three passed boards' sims — which the ledger's
own rule forbids the guard from forcing.

**22. LOW, silent — `dark`: two aria strings on storm mean *not affordable*; everywhere else,
including storm's own Rustfall card class, it means *closed by the world*. Sweeper MEDIUM and
cross-seam, refuter LOW and storm-local; PARTIAL. (text TXT-4.) Note only; do not extend
`DOM_ROWS.lit` with it.** The refuter's measurement: `el.classList.toggle("dark", !c.sendable)`
on all three desk pages, `sendable:false` a route constant — the same place, closed by the same
raiders, as the city's `"Rustfall. Dark."`.

**23. LOW, silent — the `.disabled =` gate convention: 26 script writes on the desk, zero on the
city, and only three of eight city tests forbid the script write. Refuter-only (wiring). Note;
a test-kill gap for the orchestrator.**

**24. LOW, silent — `lands` (deposit into the stores, line/storm) vs `LAND` (the consist
arriving, halt/site). Refuter-only (text). Note.**

**25. LOW / instrument — `has-do` and the 71 tokens like it: perfectly uniform on all six (seven)
city boards and completely invisible to the guard. Agreed. (classes CLS-7.)** The refuter's one
caveat governs the disposition: pin the meaning and the board set, not the line numbers.

**26. LOW / already covered — `at-halt`: dead on five boards *(still-standing:305 too — superseded
at landing; this read "four")*, live on mosswake, exactly as
`DOM_ROWS.home` already says in prose. Agreed; not a finding. (classes CLS-3.)** The extension is
real — on herbs-larder and they-remember the rule is dead for the stronger reason that they
assign the bare literal `"home"` unconditionally — and the correct change is to the instrument
(a CSS-selector set), so the prose becomes a graded row.

**27. LOUD, LOW — `terrace` on the wiring surface (W5).** Merged into finding 4.

**NONE, recorded as results, not findings:** `gone` (CLS-4 — uniform meaning; two of six sites
unreachable; row carried LOW for the reachability note alone); `glass`'s dangling rule as a
dangling rule (IDS-5 — no live failure); the card selector `button.card` vs
`button.card[data-route]` (W6 — sweeper MEDIUM, refuter NONE; by the brief's own rule no reachable
wrong action; intra-DESK); `wait` on the wiring surface (W7 — bound to nothing anywhere, named
on heat alone behind a 1500 ms wall clock, already `ROWS.wait`); `bill` (TXT-6 — ordinary
polysemy, both senses already coexist inside CITY); `HOLD`/`REPAIR` and the public/dawnspur
faces (TXT-7 — DESK↔PUBLIC_ONLY, already excluded by name); `stake` on still-standing (same
sense as dispatch; the "DESK-only" clean result is false at HEAD and the seam count is 75);
`waits` (out-of-scope vs sequenced; no rule keys on it); and the classes sweeper's partition
that does not sum (31+48+24+3 = 106 of 119 — the missing 13 are DESK+PUB 12 and CITY+PUB 1, a
bucket it never named; and DYNAMIC sites are 8, not 11, with a 186-of-187 coverage audit behind
the 8).

## Every token shared across the seam, with a verdict

The DOM surfaces (ids, classes, wiring) share 19 distinct tokens across the declared seam in
some channel; the text surface shares 69 words at twelve boards (75 at thirteen). Every DOM
token is listed; the text words are listed where a reader judged them and summarised where the
sweeper ruled them function words.

| token | channel(s) | DESK | CITY | verdict |
| --- | --- | --- | --- | --- |
| `app` | id | all 5 | all 8 | **clean** — the page root, CSS-only, byte-identical rule, nothing bound |
| `strip` | id | all 5 | all 8 | **clean** — the scene container; scripted on four pages identically (`getBoundingClientRect`); nothing bound |
| `hud` | id | all 5 | all 8 | **not a seam finding (LOW, intra-DESK)** — the readout itself on heat and public/dawnspur, a container elsewhere; see finding 17 and *do not average* |
| `marks-line` | id | 4 (not heat) | all 8 | **clean in meaning** — the marks readout everywhere; a 2-vs-9 pluralisation dialect inside DESK changes wording only |
| `train` | id, wiring | all 5 | site | **already guarded** (`DOM_ROWS["train/consist"]`, LOW) — amend prose: scenery on six, a `commitLand` control on site after `commitSite`, the only id whose node is swapped mid-sitting |
| `platform` | id | dispatch, line, storm | site, halt (absent on the six map boards) | **finding 7** — row |
| `hearth` | id / class | id on dispatch, line, storm | id on site; class on halt + six map boards | **finding 5** — row (id half); class half a comment |
| `terrace` | id, wiring | line, storm (gauge) | site (one bit), halt (a control) | **finding 4** — row |
| `dim` | class (toggle) | dispatch, line, storm (styled) | six map boards (unstyled, constant) | **finding 2** — row |
| `gone` | class (toggle / className) | dispatch, line, storm, heat | site, halt (unreachable branch) | **clean in meaning; LOW row** for the reachability note (finding "NONE" list) |
| `on` | class (toggle) | heat, scale, dispatch, line, storm | site | **clean** — the current pick, the same `var(--sel)` ring on all six; but see `live` (finding 11) for the same ring under another name on site |
| `glass` | id / class | id on heat, scale, dispatch (+ dead rule on line, storm) | class on halt + six map boards | **finding 18** — comment, not a row |
| `haul` | id / class | id on heat, scale | class on six map boards | **finding 12** — refuter-only row |
| `home` | id / class | id on heat, scale | class on all 8 | **already guarded** (`DOM_ROWS.home`, HIGH); ids and wiring surfaces confirm the row exactly; the text sweep's rendered-word split refuted as separate |
| `stack` | id / class | id on dispatch, line, storm | class on halt + six map boards | **finding 13** — refuter-only row |
| `step` | id / class | id on heat | toggle on halt, site | **finding 3** — row |
| `pointerdown` | event | all | all | **clean** — the only control event in the corpus, bound through the same `tap()` helper on all 15 pages |
| `touchstart` | event | all | all | **clean** — bound on `document`, body byte-identical, all 15 pages |
| `tap` | helper | all | all | **clean** — semantically identical on all 15 (pointerdown, preventDefault, fn); syntax varies only |
| `lit` / `unlit` | class | `unlit` on dispatch, line, storm | `lit` on halt, two-ways, still-standing | **already guarded** (`DOM_ROWS.lit`, MEDIUM) — the classes sweep reproduces the row token-for-token; the lamp-parts renames (finding 15) are a prose amendment |
| `train` (as prose) | text | dispatch, line, storm (rendered) | dice, two-ways, still-standing (unrendered) | **finding 21** — DAVID |
| `Halt` / `Dawnspur Halt` / `the Halt` | text (title, card face, aria) | heat, scale, dispatch, line, storm | all 8 | **finding 1 — HIGH** — instrument |
| `home` (word) | text | dispatch, line, storm | all 8 | **folded into finding 1** — one meaning (the return point); the name of the place is what swaps |
| `UP` / `up` | text | scale, line, storm | halt, herbs, they-remember, dice, two-ways, still-standing | **finding 20** — LOW note |
| `dark` | text (aria) / class | storm aria; class on the Rustfall card, all three desk boards | halt, dice, two-ways, still-standing | **finding 22** — storm-local note; agrees across the seam |
| `bill` | text | scale | halt, site | **clean** — ordinary polysemy, both senses already inside CITY |
| `stake` | text | dispatch, line (`stake undefined` on line — finding 10) | still-standing | **same sense across the seam**; the intra-DESK defect is finding 10 |
| `lands` / `LAND` | text | line, storm (deposit) | halt, site (arrival) | **finding 24** — LOW note |
| `waits` | text | dispatch, line, storm (the Rustfall note) | halt | **clean** — no rule keys on it |
| `SEND` | text | dispatch, line, storm | mosswake, dice, two-ways | **clean** — dispatch the vehicle, both sides |
| `Mosswake` / `Rustfall` / `Raiders hold the yard road.` | text | all desk route cards | dice, two-ways (+ still-standing) | **clean** — destinations on both sides; the raiders line byte-identical on five boards |
| `marks` / `mark` / `0 marks` | text | all | all | **clean** — one currency; digits on the desk, words on the city, no rule keys on it |
| `hold`, `full`, `out`, `short`, `here`, `took`, `sitting`, `way`, `leave`, `cover(ed)`, `lost`, `raiders`, `road`, `yard`, `dice`, `ground`, `greenhouse`, `food`, `three`, `two`, `once`, `nothing`, `something` | text | — | — | **clean** — same sense on both sides or ordinary polysemy carrying no rule (sweeper, unchallenged) |
| 24 function words (`a`, `and`, `the`, `is`, `to`, …) | text | — | — | not judged |

## Refuted at review — do not re-find

- **IDS-1's prose bridge.** "The only governing sentences about a terrace in the design record
  are the DESK ones" is false: cfd-205-halt-beat.md carries eight in the city sense, all
  correct; the one desk-quantity quotation in a city beat (cfd-209:281) attributes itself to the
  line board in the same sentence. The sweeper presented a null as evidence for HIGH. The token
  finding survives at MEDIUM.
- **IDS-2 as a cross-lineage finding.** `#hud` is a container on all seven (eight) CITY pages
  and four of five DESK pages; the leaf shape is heat plus public/dawnspur. Intra-DESK, by the
  sweeper's own rule applied to `marks-line` one item later. And the one-line copy is a two-line
  copy: no container page binds a `hud` identifier (capture only at heat:171,
  public/dawnspur:167), so the described line resolves nothing without its capture — or
  survives only via Window named access, flagged by the refuter as spec, not measurement.
- **IDS-4's "the one desk board that exports `hearth` is scale."** heat exports it too (opening
  `"off"`); corroborated by the ledger's own `UNADJUDICATED.desk` entry, whose contract needs two
  boards and is green.
- **IDS-5's "intra-DESK rather than across the seam."** `glass` is a class on six (seven) city
  boards. Three shapes, the `home` structure.
- **CLS-2's evidence drive.** Three of the four heat verbs it reports driving "from the opening"
  are refused there and change nothing; `commitWarm` sets `step="out"` once it is reachable, and
  `wait()` moves it to `"gone"`. The conclusion stands on the ledger's declared walk, not on
  that drive.
- **CLS-5's DESK-id / CITY-class framing.** `sit/dawnspur-site/index.html:235 <span
  id="hearth">` — site is CITY and takes the desk shape. The refuter's class-vs-id cross found
  12 crossing tokens where the sweeper's desk-vs-city lens found 6.
- **CLS-6's open question, half of it.** "Unwritten art" needs a flag that can move; 19 of the 20
  board/flag pairs have no writer anywhere. Only herbs-larder's `neighborAgain` transitions.
- **W1's "a desk-shaped control added to a city board."** Nine of eleven desk verb ids name a
  method no city board exports (loud); `id="send"` is suite-forbidden on seven of eight city
  boards' markup and `/TRIM/i` on the eighth; the verbatim dispatch handler references the
  page-local `pick` and throws. One silent cell survives: `#send` on still-standing.
- **W2's HIGH.** The half that is HIGH — a desk-spelled `commitSend` on a city board sends to
  Mosswake and returns true — is `ROWS` canSend/commitSend #2, adjudicated on the export surface.
  The carrier split is the new half and it is MEDIUM.
- **W6 as MEDIUM.** By the sweeper's own account the mis-bound picker does nothing: reads null,
  returns, the verb's own handler still fires. NONE, intra-DESK.
- **TXT-2 as a separate finding.** `home` means the return point on both sides; `68 percent
  home` is `Home she comes. 64.` The swap is the Halt's name (TXT-1). Do not add a `sentences`
  set to `DOM_ROWS.home`.
- **TXT-4's cross-seam `dark`.** The desk's primary `dark` is `classList.toggle("dark",
  !c.sendable)` on the Rustfall card (dispatch:334, line:408, storm:486), a route constant — the
  city's `"Rustfall. Dark."` sense exactly. The affordance sense is two aria strings on storm.
- **TXT-5's citations.** `test/dice-at-the-places.test.js:481` pins the failure sentence, not the
  desk-voiced one; no dice test pins it. two-ways pins it at `:788` and the endSentence-unread
  assertion is `:851` — the sweeper's `:775` / `:838` are 13 lines stale because that file moved
  19 lines between `919b6be` and HEAD.
- **TXT-6 / TXT-7 as LOW.** NONE: ordinary polysemy already inside CITY; and DESK↔PUBLIC_ONLY,
  which `PUBLIC_ONLY.dawnspur` already excludes by name and reason.
- **The text sweep's "provisions, stake, toll, pays, percent, odds, banked are DESK-ONLY in
  rendered text."** False at HEAD in one word: still-standing renders `"The runs took the
  stake."` Same sense; the seam count is 75, not 69.
- **The classes sweeper's own first instrument, and the refuter's.** Both harvested `"rs"` /
  `"lv"` out of concatenations and reported live rules dead; both caught it by re-reading
  output. The refuter also had a systematic off-by-one on every CSS line number (leading
  whitespace) — the sweeper's numbers were right, its own were wrong until fixed.
- **W4's apparent double bind on site.** `tap(foundryEl, …)` at :328 and `asButton`'s rebind at
  :281 do not both attach: `livePlace()` opens on `"foundry"` and :232 is already a `<button>`,
  so `asButton` returns early. Verified by the sweeper from source and by the refuter by
  execution (exactly two bindings at setup).

## Not swept

- **`still-standing` on the ids and classes surfaces, for meaning.** Both those refuters predate
  `4b08539`. This synthesis derived its three graded sets at HEAD (it has `platform-halt` /
  `platform-moss`, `dim` / `quiet` / `holds` / `neighbor` / `remembered` / `hauled` / `lit` toggles,
  `home` className, `.glass` / `.stack` / `.hearth` / `.haul` / `.larder-haul` spans, `#hud`
  containing `#marks-line`, no `#terrace`, no `#platform`, no `id="send"`) and verified the three
  facts the rows depend on (the `dim` toggle at :427 with no rule and no writer; no rule for
  `quiet` / `holds` / `neighbor`). Its per-token *meanings* on those two surfaces were not read
  by anyone and the rows say so where they name it.
- **`public/convoy-stop`'s control-to-method map, and its reachable text.** Its sim exports `[]`
  and prints a self-test at require time; its 20 `.cell` + 2 `.pad` dynamic bindings reach no
  board object. Null, not zero. Its five css-only class tokens (`gun`, `mortar`, `spike`, `wall`,
  `primary`) are targets of `"piece " + sl.kind` and are UNDERIVABLE.
- **The channels outside the instrument** — CSS selectors (the four dead `#consist.at-halt`
  rules and the dead `#glass` rules; the seventeen applied-never-styled cells), `classList.add` /
  `remove` (`has-do`), static `class=""` attributes (every scenery part's class half), the
  `bindings` and `data-*` surfaces (W1, W2, W3's control role), and `labels` / `sentences`
  (TXT-1) — were swept by the pairs and cannot be *graded* by the guard today. Their findings are
  recorded above; their rows are not built, on purpose.
- **Whether the missing `touchend` double-tap guard on the seven (eight) city pages permits
  double-tap zoom on a device.** Present and byte-identical on all five desk pages and both
  public-only pages, absent on every city page; no device was driven and no claim is made.
- **Whether `hud.textContent = …` on a container page actually deletes `#marks-line` in a
  browser.** Standard DOM semantics, flagged by both readers as mechanism rather than measurement;
  no browser was driven (out of scope).
- **The reachability of `#step.gone` by the ids refuter's walker** — its instrument calls only
  `commit*`, and heat's `wait()` is what moves `out` to `gone`; that refuter reported null. The
  classes refuter reached `"gone"` by driving `wait()`, so the fact is established by the other
  pair and the null is recorded as an instrument artefact.
- **Linear.** Nothing here reads it; no issue numbers are asserted beyond those the sources carry.
- **`out` and `in` on the class surface — found at review, 2026-09-08.** `out` is a class on four
  DESK pages with two live, styled, unrelated meanings: `button.card.out` (dispatch:156, line:200,
  storm:232, from `c.out` — the send is in the air) and `#step.out` (heat:83, from
  `board.step === "out"` — the step is on the ground); the sweep saw `out` only on the text surface.
  `in` is `#town-food.in` on halt and site and `#town-herbs.in` on the six map boards, and mosswake
  alone drives it from `board.herbsHome` where the other five use `herbsInLarder`. Neither is built as
  a row; the inclusion rule (intra-DESK refused for `hud`, admitted for `go`) is not uniform, and the
  class surface's coverage is narrower than its counts read.
- **still-standing's name surface, measured at review.** Token for token identical to
  two-ways-from-here across `ids`, `toggles`, `classNames`, static `class=""` attributes and `<style>`
  class selectors — zero differences either direction — and all fifteen class-assignment expressions
  are identical strings; `:435` drives `hauled` from `board.haulOnConsist` exactly as two-ways:427.
  The three hedges above on `platform`, `dim` and `haul` are closed for the name surface; the null
  stands for *meaning*, which no reader of those two surfaces read on C14.
- **Two phantom tokens in the instrument.** `deriveDom()` harvests `rs` (line/scale/storm toggles,
  from `classList.toggle("rs" + i, …)`) and `lv` (line/scale/storm classNames, from
  `.className = "lv" + board.level`) out of concatenations. No row keys on either; a future one would
  grade a token that does not exist.

## Do not average these

- **IDS-1 `terrace`** — sweeper HIGH, refuter MEDIUM. The fact is agreed; the HIGH rested on a
  prose-bridge argument that is a null. Carried MEDIUM, and the ledger's `lit` is the calibration
  the refuter cited: same class token, same idiom, no structural tell, MEDIUM.
- **IDS-2 `hud`** — sweeper HIGH, refuter LOW. Not a disagreement about the mechanism (both
  agree the write empties the container and detaches `#up` on scale); a disagreement about
  whether an intra-DESK split is a finding under a cross-lineage brief. Recorded as a LOW note,
  no row. **If the orchestrator wants it guarded, it is the marks-line dialect note's neighbour,
  not a HIGH row.**
- **CLS-5 / IDS-5 / classes-refuter M4** — sweeper LOW (four tokens, one comment), refuter
  MEDIUM-and-rescoped (the intra-CITY halt/site mirror is where the danger is; and `glass`'s
  residue is not inert). Built as **one comment** carrying both halves, and `hearth`'s id half
  as its own row because that half does cross the seam and does invert meaning.
- **W1 verb-dispatch** — sweeper HIGH over "a desk-shaped control", refuter MEDIUM over one
  measured cell. Carried MEDIUM; the one cell is a test edit (below).
- **W6 card selector** — sweeper MEDIUM, refuter NONE. Carried NONE by the brief's rule.
  Recorded so the difference between the two selectors is not re-discovered as a defect.
- **TXT-5 the desk voice in dead prose** — sweeper MEDIUM with a cheap red-first guard, refuter
  LOW because nothing renders or quotes it. Both agree the guard would be red on three passed
  boards and can only go green by editing them. That makes it David's, not a severity call.
- **CLS-6 dead wiring vs unwritten art** — the readers do not disagree; the sweeper declined to
  guess and the refuter supplied the discriminator (no writer on 19 of 20 pairs). It is listed
  here because the remedy is opposite under the two readings and neither reader has the
  authority to pick. David's.
- **CLS-7 `has-do` as a row** — sweeper proposes a row to extend the instrument on; refuter
  agrees the token and warns the row as written pins twelve line numbers that move with every
  `paint()` edit. Not built; the instrument extension is built first or not at all.

## David's calls — listed, never built

Six. Each line: what the surface shows · what the source says · what David would rule.

1. **`/dawnspur-line/` speaks "stake undefined."** · `sit/dawnspur-line/index.html:419` reads
   `c.stake`; line's cards carry no `stake` (its stake is food, `.cs` uses `stakeText`); every
   sendable card's aria-label says the literal word `undefined` at every frame, to screen-reader
   players only · The fix is one line in a PASSED, hash-pinned board (hub card + descendants).
   Re-hash and recut the pins, or leave the passed bytes and record the defect? The ledger's rule
   is that a guard never forces a board edit; this is not a guard, it is a shipped a11y defect.

   *(2026-09-09, by counsel on David's instruction — entry 42 of
   `docs/decisions-open-2026-09-02.md`. RULED: (b) — leave the passed bytes, record the defect;
   the rule question is returned to David. Option (a) as this call states it is a partial fix:
   storm's bare `stakeText(c)` still speaks the wrong thing on a sendable-but-unlit card; the
   parity form (mirroring `index.html:404`) is `c.condition === null ? stakeText(c) :
   c.condition`, not `stakeText(c)` alone. Full parity patch and pin census recorded at finding
   10, above; also newly measured there, `/dawnspur-storm/` carries the lesser half of the same
   divergence, unfixed. A self-cancelling guard, green at HEAD and red on the fix, is ORDERED by
   this ruling and **NOT built** — it is owed as an append to `test/dawnspur-line.test.js`,
   outside this batch's glob, and must be mutation-verified red-first before it lands
   *(corrected at the fix pass, 2026-09-09; this read "is built in
   `test/dawnspur-line.test.js`", and that file is unchanged at `e226a9f`)*.)*
2. **The desk voice in three city sims** — `"The train brought the Mosswake cargo home."` at
   dice:322, two-ways:431, still-standing:601, and site's three unrendered `*_SENTENCE`s ·
   unrendered everywhere, quoted in no document, and pinned verbatim by one passed board's test
   (`test/two-ways-from-here.test.js:788`) *(superseded at landing; this read "unquoted everywhere")*;
   `ROWS.runSentence` / `endSentence` are BENIGN on
   the *read* question · Is dead desk-voiced prose in a passed board's sim pinned evidence to
   leave, or a re-voicing owed before the next city board inherits it a fourth time? A
   `doesNotMatch` guard is red on three boards today and cannot go green without three board
   edits and one test edit.

   *(2026-09-09, by counsel on David's instruction — entry 43 of
   `docs/decisions-open-2026-09-02.md`. Two claims in this row are corrected; the disposition —
   leave the three boards, do not build an enforcing guard yet, the scoping convention is
   David's to set — is unchanged. (i) The `doesNotMatch` guard claim is backwards. Verified at
   `e226a9f`: the sentence is composed at RUNTIME from `MOSSWAKE.cargo` —
   `sit/dice-at-the-places/sim.js:322`, `sit/two-ways-from-here/sim.js:431` and
   `sit/still-standing/sim.js:601` each read `s.sentence = "The train brought " +
   MOSSWAKE.cargo + " home."` — so a guard over the SOURCE text is GREEN on all three boards
   today and would stay green on a fourth board composing it identically; only a guard over the
   DERIVED `runSentence` export is red where it should be. (ii) "Quoted in no document" is
   false: the sentence is quoted in this row itself (above) and twice inside
   `docs/decisions-open-2026-09-02.md`. Verified: `test/two-ways-from-here.test.js:788` pins the
   derived value, `h.b.runSentence, "The train brought the Mosswake cargo home."`; no page
   renders it, and the city already has a rendered voice for the same moment —
   `test/two-ways-from-here.test.js:794`, `"The consist is home. The haul is on it."`)*
3. **`quiet` / `holds` / `neighbor` / `dim` on the city boards** — toggled on `#mosswake` and
   `#halt` by six boards for `dim`, five for `quiet`, five for `holds` and four for `neighbor`; no
   rule anywhere; no writer on 23 of 24 board/flag pairs, and the one writer — herbs-larder's
   `neighborAgain`, `sim.js:132` — is on the one map board that does not toggle `neighbor`, so the
   only flag that moves reaches no class on any page *(superseded at landing, measured at `0ffd1aa`;
   this read "on six boards, no rule anywhere, no writer on 19 of 20 board/flag pairs (only
   herbs-larder's neighborAgain moves)")* · dice:88 / :247 carry art-direction comments naming the
   states · Dead wiring (delete the toggles) or unwritten art (write the rules)? Opposite
   remedies; the one moving flag was the only evidence for the second reading, and it does not
   connect to a toggle.

   *(2026-09-09, by counsel on David's instruction — entry 44 of
   `docs/decisions-open-2026-09-02.md`. Three claims in this row are corrected; the disposition
   — no passed board edited, the remedy (drawing rules, or drop the toggles) returned to David —
   is unchanged. The reading is closer to UNWRITTEN ART than to dead wiring. (i) Verified at
   `e226a9f`: the four flags are initialised TRUE at the opening on the four city boards that
   carry all four — `sit/dice-at-the-places/sim.js:444,449,450,454`, `two-ways-from-here` at
   `:590,595,596,600`, `still-standing` at `:795,800,801,805` and `they-remember` at
   `:206,211,212,217`, each opening `haltHolds`, `mossDim`, `mossQuiet` and `neighborAgain` all
   true. **Two boards are exceptions, and the first cut of this correction swallowed them**
   *(corrected at the fix pass, 2026-09-09; this read "on every board that carries them", which
   `sit/herbs-larder` falsifies)*: `sit/herbs-larder/sim.js:215` opens `neighborAgain` **false**
   — and `:132`, the one writer this row cites, is what sets it true — while
   `sit/mosswake-loop/sim.js` carries only two of the four, `haltHolds` at `:229` and `mossDim`
   at `:234`, both true. Exactly ONE line across all six city sims ever
   assigns any of the four, `sit/herbs-larder/sim.js:132`. So "no writer on 23 of 24
   board/flag pairs" understates what is missing: the classes are applied at every frame: what
   never happens is a TRANSITION, not the state. (ii) The denominator above (six for `dim`, five
   for `quiet`, five for `holds`, four for `neighbor` — 20 pairs) is already correct; "23 of 24"
   is the figure that does not follow from it. (iii) "No rule anywhere" is true of `.dim` /
   `.quiet` / `.holds` / `.neighbor` on city pages, but not of every page: `#ladder.dim` IS
   styled, on all three desk boards — verified `sit/dawnspur-dispatch/index.html:100-101`,
   `sit/dawnspur-line/index.html:102-103`, `sit/dawnspur-storm/index.html:102-103` — so a
   drawing rule for the same shape of state exists elsewhere in the corpus. The dim guard row's
   own comment at `test/lexicon-ledger.js:1136-1139` already records this same measurement.)*
4. **`dawnspur-site`'s odd shape** — the one CITY board whose scenery parts (`scaffold`, `works`,
   `hearth`, `bill`) are page-unique ids while halt and every map board scope classes inside a
   named building; also the one board that carries `train`, `hearth`, `platform` under the desk's
   ids · site passed (2026-08-30, recut 2) and is pinned · Is site's shape a lineage-ancestor
   convention every future city board must avoid copying, or an accepted one-off? The answer
   decides whether the *SCENERY PARTS* comment above `DOM_ROWS` is a warning or a rule.

   *(2026-09-09, by counsel on David's instruction — entry 45 of
   `docs/decisions-open-2026-09-02.md`. The premise this row rests on is corrected: `site
   passed (2026-08-30, recut 2) and is pinned` is FALSE on the pass clause. Verified at
   `e226a9f`: `public/index.html:73` reads "live — sat 2026-08-30, not passed"; `KILLS.md:55-66`
   carries dawnspur-site as a kill entry — "Sat 2026-08-30, not passed... Do not recut this
   path; the pins stand." Site's ids are pinned as shipped bytes, but the board is not a passed
   ancestor; it is a failed sit in the kills ledger. That changes the entry's own risk
   calculus: canon's "A system is inherited only if it passed a sit. A killed system does not
   travel" (`docs/mechanisms-recommitted.md:414-416`) already does forward work here without a
   council-invented rule. Disposition ACCEPTED ONE-OFF (option b): the ruling **directs** that
   the *SCENERY PARTS* comment above `DOM_ROWS` (`test/lexicon-ledger.js:861-891`, outside this
   batch's glob and **not amended here**) be made to state, as fact and not as a rule, that site
   was sat 2026-08-30 and not passed and is a `KILLS.md` entry, and that its page-unique scenery
   ids are the pre-pattern shape of the first city board *(corrected at the fix pass,
   2026-09-09; this read that the comment "states plainly … that site is a one-off that did not
   pass" — the comment is unchanged at `e226a9f`, records site only as "a CITY board that takes
   the DESK shape", and says nothing about the sit)*; the prescriptive form — a binding rule for
   boards not yet cut — is returned to David.)*
5. **The rendered name of the Halt** — `<title>Dawnspur Halt</title>` on heat, scale and halt;
   `DAWNSPUR HALT` as a destination card face on three desk boards; `the Halt` as home on seven
   city aria-labels; storm renders both senses in one line · `ROWS.halt` (HIGH) grades ids only
   · This is the audit's #4 one layer out and the sweep's only agreed HIGH. Is the desk's card
   face to be re-voiced (a board edit on three passed boards), or is the split to be carried as
   a declared row once a `labels` deriver exists? Neither is a worker's call.
6. **`test/still-standing.test.js` lacks the `id="send"` markup kill** the seven other city tests
   carry, so a desk `#send` control is the one silent copy in the corpus that lands suite-green ·
   the kill is a four-token regex in every other city test · Add it (a test-only edit, no board
   bytes move), or is still-standing's test deliberately narrower? · **Premise refuted at review,
   and the kill built anyway at `925d23a`** *(superseded at landing: a desk `#send` BUTTON on this
   board is LOUD — `test/still-standing.test.js:1470-1472` enumerates every button id and `:1627`
   kills the desk shape — so there was no silent copy, and "the seven other city tests carry the
   regex" is five. `git log` records no reason for the omission. The regex is added for uniformity
   and for a non-button element carrying the id, which the enumeration does not count. David can
   strike it. This listed the call as his "because the pattern of that file's kills was chosen by
   whoever landed C14".)*

Also for him, from the audit's standing list and not this sweep: whether the storm's stop and
the site's stop belong in `KILLS.md` — unchanged, and this sweep adds nothing to it.

## Dispositions

**Nothing is written to either repository by the synthesis** *(the landing is `925d23a`; see the rows bullet)*. `git status --porcelain` in
`C:\dev\skyrail-boards` is empty; no `sit/`, `public/`, `test/` or `docs/` byte moved under
this synthesis; no git write, server, browser or QA harness ran. The suite was run once,
read-only: 814 / 814 at `c8c4546`.

- **`C:\tmp\domsweep\results.json` — the four pairs' results, verbatim, 247,681 bytes.** Written
  first and worked from; parses; four surfaces, 26 findings, 26 verdicts, 15 missed. Nothing in
  what was received looked cut off.
- **`test/lexicon-ledger.js` `DOM_ROWS` — the 11 rows, landed at `925d23a`, with the coverage-map
  entries in `test/lexicon.test.js` that stop the ledger dropping one** *(superseded at landing;
  this read "C:\tmp\domsweep\proposed-dom-rows.js — 11 rows in test/lexicon-ledger.js's own shape,"
  "in two labelled sets"; the file was the author's paste source and is scratch)*. `DOM_ROWS_AGREED` (7): `terrace`, `platform` (+ `platform-halt`,
  `platform-moss`), `hearth`, `step`, `dim`, `gone`, `foundry`. `DOM_ROWS_REFUTER_ONLY` (4):
  `live`, `haul`, `stack`, `go`. Every board list re-derived at `c8c4546` over thirteen boards
  and graded with the exact loop `test/lexicon.test.js` uses — the file self-checks when run
  from the repo root: **`11 rows (7 agreed + 4 refuter-only), 0 drift over 13 boards`**, exit 0,
  0 NUL, 0 CR. Every row carries only the three graded sets; where a finding's other half lives
  in a set the instrument cannot see it is a comment marked UNGRADED, so it is neither
  re-found nor mistaken for graded. Each `by:` cites this record's landed section and finding number
  — `gone` cites result CLS-4, which is not numbered — and says "a worker's read, not a ruling"
  *(the proposal file's numbers were stale for three rows and its refuter-only labels R1–R4 appear
  nowhere in this record; corrected by the author at landing)*. Three prose amendments to existing rows (`train/consist`,
  `lit`, `home`) and the one *SCENERY PARTS* comment are in the same file, below the rows,
  as text for the guard author — they change no set and cannot change what grades.
- **Instrument extensions owed, six, ranked by what each would make gradeable.** (1) A
  `cssClasses` set from `<style>` selectors — 20 lines, no `@media` in the corpus — makes the
  `at-halt` prose and the dead `#glass` / applied-never-styled findings graded rather than
  comments. (2) An `attrClasses` set from static `class=""` attributes — the class half of
  `hearth` / `haul` / `stack` / `glass` and the lamp-parts renames. (3) `adds` / `removes` from
  `classList.add/remove` — `has-do`, the clean worked example. (4) `dataAttrs` — W2's carrier
  split. (5) `bindings` — W1, W3, W4's control role; the wiring refuter's executing harness
  (page under `node:vm` with a stub DOM, 15/15 pages, zero unknown-property gaps) yields it
  directly and, unlike a textual set, sees site's `asButton` rebind. (6) `labels` / `sentences`
  from rendering — TXT-1, the sweep's only HIGH — **together with the test loop's set list**,
  which is hard-coded and skips unknown keys. Until (6) lands, finding 1 is a record entry and
  nothing more, and the record says so rather than proposing a decorative row.
- **Two board-local test edits, for the orchestrator's landing queue, not this synthesis:** the
  `id="send"` kill in `test/still-standing.test.js` (David's call 6 — **built at `925d23a`**, its premise
  refuted at review, see the call); and the
  `.disabled =` script-write kill on the five city tests that forbid only the attribute (halt,
  mosswake, herbs, they-remember, dice) — the desk idiom copied there lands green and produces
  the dead button two of the lineage's own tests name as forbidden.
- **Three figures to re-publish where they are published.** Audit §3.2's "`data-action`
  attributes" (none exist; the surface is `data-route` and `data-i`) and "4,860 lines" (5,808
  for `sit/` at HEAD); and the C13 delta's "C13's 30 DOM ids are exactly dice's set" —
  re-derived here and still true, worth saying because it had not been re-derived since.
- **Withdrawn / merged before building:** W5 into finding 4 (same token, two surfaces); the
  ids-refuter's `step` into finding 3; TXT-2 into finding 1; CLS-5, IDS-5 and the classes
  refuter's M4 into one comment; CLS-3 and W7 as already-covered results.
